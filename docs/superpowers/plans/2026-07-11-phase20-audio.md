# Phase 20 — Audio view Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Audio assets trong project (import/lưu/mở), section AUDIO trong tree, waveform trên track events của dopesheet, phát tiếng event khi play/scrub với volume/balance, và MCP `import_audio` (62 tools).

**Architecture:** Module singleton `src/audio/engine.ts` (Web Audio thuần) decode + cache AudioBuffer, cấp peaks cho waveform và play qua Gain→StereoPanner. Store thêm `audioAssets` đi vào `ProjectPayload` (field optional — backward compatible). TimelinePanel bắt event keys giữa 2 tick RAF/scrub để phát tiếng. Core KHÔNG đổi.

**Tech Stack:** TypeScript strict, React, zustand, Web Audio API (không dependency mới).

## Global Constraints

- Spec: `docs/superpowers/specs/2026-07-11-phase20-audio-design.md`.
- Mọi lệnh pnpm chạy từ `client/`; core/pytest không đổi.
- KHÔNG dependency mới; KHÔNG đổi selector e2e cũ ("Edit", "Done", "New", "Play", "Pause", `.tree`, `.row.bone`, `.ruler`, `.track .key`, `.timeline-header`).
- Event `footstep` trong bridge.mjs giữ nguyên (assertion cũ) — audio test dùng event mới `clang`.
- Commit message kết thúc bằng `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Format trước mỗi commit: `pnpm exec prettier --write <files>`; lint: `pnpm lint`.

---

### Task 1: Nền tảng — AudioAsset store + persistence + audio engine

**Files:**

- Create: `client/packages/editor/src/audio/engine.ts`
- Modify: `client/packages/editor/src/state/store.ts` (AudioAsset, audioAssets, actions, replaceProject)
- Modify: `client/packages/editor/src/state/persistence.ts` (ProjectPayload.audioAssets?)
- Modify: `client/packages/editor/src/state/actions.ts` (saveProjectFile)
- Modify: `client/packages/editor/src/App.tsx` (autosave payload + autosave load)
- Modify: `client/packages/editor/src/server/project-sync.ts` (collectPayload)
- Modify: `client/packages/editor/src/components/Toolbar.tsx` (onOpenProject/onNewProject/onImportSpineJson gọi replaceProject 3 tham số)

**Interfaces:**

- Produces (Task 2–4 dùng):
  - `AudioAsset { name: string; dataUrl: string }` (export từ store.ts)
  - store: `audioAssets: Record<string, AudioAsset>`; `addAudioAssets(assets: AudioAsset[]): void`; `removeAudioAsset(name: string): void`; `replaceProject(json, assets, audioAssets?: AudioAsset[])`
  - engine (default export object `audioEngine`): `ensure(name, dataUrl): void`, `duration(name): number | null`, `peaks(name, buckets): Float32Array | null`, `play(name, opts?: { volume?: number; balance?: number; rate?: number }): void`, `stopAll(): void`, `remove(name): void`, `setMuted(m: boolean): void`, `isMuted(): boolean`, `onDecoded(cb: () => void): () => void`

- [ ] **Step 1: store.ts** — thêm sau `ImageAsset`:

```ts
/** Imported audio clip (event sounds); dataUrl is persisted in the project file. */
export interface AudioAsset {
  name: string;
  dataUrl: string;
}
```

Thêm field (cạnh `assets:`): `audioAssets: Record<string, AudioAsset>;` — khai báo actions (cạnh `addAssets`):

```ts
addAudioAssets(assets: AudioAsset[]): void;
removeAudioAsset(name: string): void;
```

Sửa chữ ký `replaceProject(json: SpineJson, assets: ImageAsset[]): ValidationIssue[];` → `replaceProject(json: SpineJson, assets: ImageAsset[], audioAssets?: AudioAsset[]): ValidationIssue[];`

Initial state: `audioAssets: {},` (cạnh `assets: {},`).

Impl (cạnh `addAssets`):

```ts
addAudioAssets: (assets) =>
  set((s) => {
    const next = { ...s.audioAssets };
    for (const asset of assets) next[asset.name] = asset;
    return { audioAssets: next };
  }),
removeAudioAsset: (name) =>
  set((s) => {
    const next = { ...s.audioAssets };
    delete next[name];
    return { audioAssets: next };
  }),
```

`replaceProject: (json, assets) =>` → `(json, assets, audioAssets = [])` và trong `set((s) => ({ ... assets: ..., ` thêm dòng:

```ts
audioAssets: Object.fromEntries(audioAssets.map((a) => [a.name, a])),
```

- [ ] **Step 2: persistence.ts** — `ProjectPayload` thêm dòng (sau `assets: ImageAsset[];`):

```ts
/** Optional since P20 — older project files simply have no audio. */
audioAssets?: AudioAsset[];
```

(import `AudioAsset` từ `./store.js` cạnh ImageAsset.)

- [ ] **Step 3: 3 chỗ build payload + các call site replaceProject**

`state/actions.ts` (saveProjectFile) — thêm vào object payload: `audioAssets: Object.values(state.audioAssets),`

`App.tsx` autosave (dòng ~90) — thêm: `audioAssets: Object.values(s.audioAssets),` — và subscribe condition dòng ~86 đổi thành:

```ts
if (
  state.revision === prev.revision &&
  state.assets === prev.assets &&
  state.audioAssets === prev.audioAssets
)
  return;
```

`App.tsx` autosave-load (dòng ~83): `replaceProject(payload.spine, payload.assets)` → `replaceProject(payload.spine, payload.assets, payload.audioAssets ?? [])`.

`server/project-sync.ts` collectPayload — thêm: `audioAssets: Object.values(s.audioAssets),`

`Toolbar.tsx`:

- onOpenProject: `state.replaceProject(payload.spine, payload.assets, payload.audioAssets ?? []);`
- onNewProject: `replaceProject(serializeSpineJson(createEmptySkeleton()), [], []);`
- onImportSpineJson (dòng ~129, giữ assets hiện tại): `replaceProject(json, Object.values(state.assets), Object.values(state.audioAssets));`

Kiểm tra còn call site nào: `grep -rn "replaceProject(" client/packages/editor/src` — nơi nào load payload server (ProjectsModal?) cũng truyền `payload.audioAssets ?? []`.

- [ ] **Step 4: `src/audio/engine.ts`** (file mới):

```ts
/**
 * Editor-only audio engine (Web Audio): decodes audio-asset data URLs into
 * cached AudioBuffers, serves min/max peaks for dopesheet waveforms, and
 * plays event sounds through gain (volume) + stereo panner (balance).
 * The AudioContext is created lazily (autoplay policy: first use follows a
 * user gesture — Play button, scrub drag, or the preview ▶).
 */

interface PlayOpts {
  volume?: number;
  balance?: number;
  rate?: number;
}

class AudioEngine {
  private ctx: AudioContext | null = null;
  private buffers = new Map<string, AudioBuffer>();
  private decoding = new Set<string>();
  private peaksCache = new Map<string, Float32Array>();
  private playing = new Set<AudioBufferSourceNode>();
  private subscribers = new Set<() => void>();
  private muted = false;

  private context(): AudioContext {
    this.ctx ??= new AudioContext();
    return this.ctx;
  }

  /** Decode (once) so duration/peaks become available; notifies subscribers. */
  ensure(name: string, dataUrl: string): void {
    if (this.buffers.has(name) || this.decoding.has(name)) return;
    this.decoding.add(name);
    void (async () => {
      try {
        const res = await fetch(dataUrl);
        const bytes = await res.arrayBuffer();
        const buffer = await this.context().decodeAudioData(bytes);
        this.buffers.set(name, buffer);
        for (const cb of this.subscribers) cb();
      } catch {
        // Undecodable codec: keep the asset (export still references the
        // file name); waveform/preview simply stay unavailable.
        console.warn(`Cannot decode audio "${name}".`);
      } finally {
        this.decoding.delete(name);
      }
    })();
  }

  duration(name: string): number | null {
    return this.buffers.get(name)?.duration ?? null;
  }

  /** Mono max-|sample| per bucket, cached per (name, buckets). */
  peaks(name: string, buckets: number): Float32Array | null {
    const buffer = this.buffers.get(name);
    if (!buffer || buckets <= 0) return null;
    const key = `${name}#${buckets}`;
    const cached = this.peaksCache.get(key);
    if (cached) return cached;
    const out = new Float32Array(buckets);
    const channels = Array.from({ length: buffer.numberOfChannels }, (_, c) =>
      buffer.getChannelData(c),
    );
    const per = buffer.length / buckets;
    for (let b = 0; b < buckets; b++) {
      const from = Math.floor(b * per);
      const to = Math.min(buffer.length, Math.ceil((b + 1) * per));
      let max = 0;
      for (const data of channels) {
        for (let i = from; i < to; i++) {
          const v = Math.abs(data[i] ?? 0);
          if (v > max) max = v;
        }
      }
      out[b] = max;
    }
    this.peaksCache.set(key, out);
    return out;
  }

  play(name: string, opts: PlayOpts = {}): void {
    if (this.muted) return;
    const buffer = this.buffers.get(name);
    if (!buffer) return;
    const ctx = this.context();
    void ctx.resume();
    const source = ctx.createBufferSource();
    source.buffer = buffer;
    source.playbackRate.value = Math.max(0.1, opts.rate ?? 1);
    const gain = ctx.createGain();
    gain.gain.value = Math.max(0, opts.volume ?? 1);
    const panner = ctx.createStereoPanner();
    panner.pan.value = Math.max(-1, Math.min(1, opts.balance ?? 0));
    source.connect(gain).connect(panner).connect(ctx.destination);
    this.playing.add(source);
    source.onended = () => {
      this.playing.delete(source);
    };
    source.start();
  }

  stopAll(): void {
    for (const source of this.playing) {
      try {
        source.stop();
      } catch {
        // already stopped
      }
    }
    this.playing.clear();
  }

  remove(name: string): void {
    this.buffers.delete(name);
    for (const key of this.peaksCache.keys()) {
      if (key.startsWith(`${name}#`)) this.peaksCache.delete(key);
    }
  }

  setMuted(m: boolean): void {
    this.muted = m;
    if (m) this.stopAll();
  }

  isMuted(): boolean {
    return this.muted;
  }

  /** Re-render hook for components drawing waveforms. */
  onDecoded(cb: () => void): () => void {
    this.subscribers.add(cb);
    return () => this.subscribers.delete(cb);
  }
}

export const audioEngine = new AudioEngine();
```

- [ ] **Step 5: Verify** — từ `client/`: `pnpm typecheck` sạch; `pnpm test` xanh (không suite mới); `pnpm --filter @spine-editor/editor build` sạch.

- [ ] **Step 6: Commit**

```bash
cd /Users/tuananh/Projects/you/spine_editor/client
pnpm exec prettier --write packages/editor/src/audio/engine.ts packages/editor/src/state/store.ts packages/editor/src/state/persistence.ts packages/editor/src/state/actions.ts packages/editor/src/App.tsx packages/editor/src/server/project-sync.ts packages/editor/src/components/Toolbar.tsx
pnpm lint
cd .. && git add client/packages/editor
git commit -m "P20: audio assets in store/project payload + Web Audio engine

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: TreePanel AUDIO section + EventDock audio select

**Files:**

- Modify: `client/packages/editor/src/components/TreePanel.tsx` (AudioSection + render + import input)
- Modify: `client/packages/editor/src/components/tree/dock/EventDock.tsx` (Audio select)
- Modify: `client/packages/editor/src/styles.css`

**Interfaces:**

- Consumes: store `audioAssets`/`addAudioAssets`/`removeAudioAsset`, `uniqueName` (store.ts), `audioEngine` (Task 1).

- [ ] **Step 1: TreePanel.tsx — AudioSection** (đặt ngay sau `ImagesSection`):

```tsx
function AudioSection() {
  const audioAssets = useEditor((s) => s.audioAssets);
  const inputRef = useRef<HTMLInputElement | null>(null);

  async function onImport(files: FileList | null) {
    if (!files || files.length === 0) return;
    const state = useEditor.getState();
    const next: AudioAsset[] = [];
    for (const file of Array.from(files)) {
      const dataUrl = await new Promise<string>((resolve, reject) => {
        const reader = new FileReader();
        reader.onload = () => resolve(String(reader.result));
        reader.onerror = () => reject(new Error(`Cannot read "${file.name}".`));
        reader.readAsDataURL(file);
      });
      const base = file.name.replace(/\.[^.]+$/, '');
      const name = uniqueName(
        base,
        (n) => n in state.audioAssets || next.some((a) => a.name === n),
      );
      next.push({ name, dataUrl });
    }
    state.addAudioAssets(next);
    for (const a of next) audioEngine.ensure(a.name, a.dataUrl);
  }

  return (
    <>
      <div className="panel-title">Audio</div>
      <div className="assets audio-assets">
        {Object.values(audioAssets).length === 0 && (
          <div className="empty">Import audio, then pick it on an event (Audio field).</div>
        )}
        {Object.values(audioAssets).map((asset) => (
          <div key={asset.name} className="asset-row">
            <span className="audio-icon">🔉</span>
            <span className="asset-name" title={asset.name}>
              {asset.name}
            </span>
            <button
              title="Preview"
              onClick={() => {
                audioEngine.ensure(asset.name, asset.dataUrl);
                audioEngine.play(asset.name);
              }}
            >
              ▶
            </button>
            <button
              title="Remove audio asset"
              onClick={() => {
                useEditor.getState().removeAudioAsset(asset.name);
                audioEngine.remove(asset.name);
              }}
            >
              ✕
            </button>
          </div>
        ))}
      </div>
      <button className="asset-import" onClick={() => inputRef.current?.click()}>
        Import Audio
      </button>
      <input
        ref={inputRef}
        type="file"
        accept="audio/*"
        multiple
        hidden
        onChange={(e) => {
          void onImport(e.target.files);
          e.target.value = '';
        }}
      />
    </>
  );
}
```

Imports thêm ở TreePanel.tsx: `audioEngine` từ `'../audio/engine.js'`; `type AudioAsset` + `uniqueName` từ store (uniqueName có thể đã import — kiểm tra); `useRef` từ react (đã có `useState`/`useCallback` — bổ sung nếu thiếu). Render `<AudioSection />` ngay sau `<ImagesSection />`.

Lưu ý search filter: ImagesSection hiện KHÔNG lọc theo search (kiểm tra — nếu tree search chỉ lọc bones/slots thì AudioSection cũng không cần; spec nói "lọc luôn audio rows (theo tên, như Images)" — nếu Images không lọc, giữ đồng bộ Images: không lọc. Ghi chú này giải quyết bằng cách LÀM GIỐNG ImagesSection hiện tại.)

- [ ] **Step 2: EventDock.tsx — Audio select** — thay block `<label className="field"><span>Audio</span><input .../></label>` bằng:

```tsx
<label className="field">
  <span>Audio</span>
  <select value={def.audio ?? ''} onChange={(e) => patch({ audio: e.target.value || undefined })}>
    <option value="">— none —</option>
    {Object.keys(audioAssets).map((n) => (
      <option key={n} value={n}>
        {n}
      </option>
    ))}
    {def.audio && !(def.audio in audioAssets) && (
      <option value={def.audio}>{def.audio} (missing)</option>
    )}
  </select>
</label>
```

Đầu component thêm: `const audioAssets = useEditor((s) => s.audioAssets);`

- [ ] **Step 3: styles.css** — cuối file:

```css
/* ---- Audio assets (P20) ---- */
.audio-assets .audio-icon {
  width: 18px;
  text-align: center;
  flex: none;
}
.asset-import {
  margin: 4px 0;
}
```

- [ ] **Step 4: Verify** — `pnpm typecheck` + `pnpm --filter @spine-editor/editor build`; dev thử import 1 file wav → row hiện, ▶ kêu, event def chọn được (manual, tùy chọn).

- [ ] **Step 5: Commit**

```bash
cd /Users/tuananh/Projects/you/spine_editor/client
pnpm exec prettier --write packages/editor/src/components/TreePanel.tsx packages/editor/src/components/tree/dock/EventDock.tsx packages/editor/src/styles.css
pnpm lint
cd .. && git add client/packages/editor
git commit -m "P20: AUDIO tree section + EventDock audio select

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: Waveform trong dopesheet + play/scrub có tiếng + nút mute

**Files:**

- Create: `client/packages/editor/src/components/EventWave.tsx`
- Modify: `client/packages/editor/src/components/TimelinePanel.tsx` (waveform render, RAF audio, scrub audio, mute button)
- Modify: `client/packages/editor/src/styles.css`

**Interfaces:**

- Consumes: `audioEngine` (Task 1), store `audioAssets`, `anim`, event keys/`doc.data.events` (sẵn có), `PAD`/`pps` (TimelinePanel).

- [ ] **Step 1: EventWave.tsx** (file mới):

```tsx
import { useEffect, useRef, useState } from 'react';
import { audioEngine } from '../audio/engine.js';

/** Waveform strip for one event key's audio, starting at the key's x. */
export function EventWave({
  name,
  left,
  pxPerSecond,
  height,
  maxWidth,
}: {
  name: string;
  left: number;
  pxPerSecond: number;
  height: number;
  maxWidth: number;
}) {
  const canvasRef = useRef<HTMLCanvasElement | null>(null);
  const [, force] = useState(0);
  useEffect(() => audioEngine.onDecoded(() => force((v) => v + 1)), []);

  const duration = audioEngine.duration(name);
  const width = duration ? Math.max(2, Math.min(duration * pxPerSecond, maxWidth)) : 0;

  useEffect(() => {
    const canvas = canvasRef.current;
    if (!canvas || width <= 0) return;
    const buckets = Math.max(1, Math.floor(width));
    const peaks = audioEngine.peaks(name, buckets);
    const g = canvas.getContext('2d');
    if (!peaks || !g) return;
    canvas.width = buckets;
    canvas.height = height;
    g.clearRect(0, 0, buckets, height);
    g.fillStyle = 'rgba(56, 117, 183, 0.55)'; // --accent, translucent
    const mid = height / 2;
    for (let x = 0; x < buckets; x++) {
      const h = Math.max(1, peaks[x]! * (height - 2));
      g.fillRect(x, mid - h / 2, 1, h);
    }
  }, [name, width, height]);

  if (width <= 0) return null;
  return <canvas ref={canvasRef} className="wave" style={{ left, width, height }} />;
}
```

- [ ] **Step 2: TimelinePanel — render waveform** — import `EventWave` + `audioEngine`; store subscribe thêm `const audioAssets = useEditor((s) => s.audioAssets);` (cạnh các selector đầu component). Trong block track events, NGAY TRƯỚC `{eventKeys.map((key) => {` thêm:

```tsx
{
  eventKeys.map((key) => {
    const def = useEditor.getState().doc.data.events[key.name];
    const asset = def?.audio ? audioAssets[def.audio] : undefined;
    if (!asset) return null;
    audioEngine.ensure(asset.name, asset.dataUrl);
    const t = key.time ?? 0;
    return (
      <EventWave
        key={`wave-${key.name}@${t}`}
        name={asset.name}
        left={PAD + t * pps}
        pxPerSecond={pps}
        height={22}
        maxWidth={Math.max(0, (span - t) * pps)}
      />
    );
  });
}
```

(`span` là biến sẵn có trong component — chiều dài timeline giây; track height 22 khớp `.track` hiện tại — kiểm tra CSS `.track` height, chỉnh 22 cho đúng giá trị thật.)

- [ ] **Step 3: CSS `.wave`** — cuối styles.css:

```css
/* ---- Event audio waveform (P20) ---- */
.track .wave {
  position: absolute;
  top: 0;
  pointer-events: none;
}
```

(Key ticks `.key` đã absolute — canvas render trước trong DOM nên nằm dưới; nếu key bị che, thêm `z-index: 1` cho `.track .key` trong bước này.)

- [ ] **Step 4: Phát tiếng trong RAF** — trong `useEffect([anim.playing])` (Playback ticker):

Sau `let last = performance.now();` thêm `let prevT = useEditor.getState().anim.time;`. Sau khi có `t` cuối (ngay TRƯỚC `s.setAnimTime(t);`) thêm:

```ts
fireEventAudio(s, prevT, t, start, end);
prevT = t;
```

(Trường hợp wrap: `fireEventAudio` nhận cả start/end để xử lý.) Cleanup effect: thêm `audioEngine.stopAll();` trong hàm return. Thêm helper cấp module (trên component, sau imports):

```ts
/** Plays audio of event keys crossed between prev and t (handles loop wrap). */
function fireEventAudio(
  s: ReturnType<typeof useEditor.getState>,
  prev: number,
  t: number,
  loopStart: number,
  loopEnd: number,
): void {
  const animName = s.anim.current;
  if (!animName) return;
  const animation = s.doc.getAnimation(animName);
  const keys = animation?.events ?? [];
  if (keys.length === 0) return;
  const crossed = (kt: number): boolean => {
    if (t >= prev) return prev < kt && kt <= t;
    // Wrapped: (prev, loopEnd] ∪ [loopStart, t]
    return (prev < kt && kt <= loopEnd) || (loopStart <= kt && kt <= t);
  };
  for (const key of keys) {
    const kt = key.time ?? 0;
    if (!crossed(kt)) continue;
    const def = s.doc.data.events[key.name];
    if (!def?.audio || !(def.audio in s.audioAssets)) continue;
    audioEngine.ensure(def.audio, s.audioAssets[def.audio]!.dataUrl);
    audioEngine.play(def.audio, {
      volume: key.volume ?? def.volume ?? 1,
      balance: key.balance ?? def.balance ?? 0,
      rate: s.anim.speed,
    });
  }
}
```

- [ ] **Step 5: Scrub có tiếng** — sửa `onScrub`:

```ts
const scrubPrev = useRef<number | null>(null);

function onScrub(e: React.PointerEvent) {
  const s = useEditor.getState();
  const t = Math.min(timeFromEvent(e), span);
  const prev = scrubPrev.current;
  if (prev !== null && prev !== t) {
    const lo = Math.min(prev, t);
    const hi = Math.max(prev, t);
    fireEventAudio({ ...s, anim: { ...s.anim, speed: 1 } }, lo, hi, lo, hi);
  }
  scrubPrev.current = t;
  s.setAnimTime(t);
}
```

Chỗ bắt đầu scrub (onPointerDown của ruler — nơi hiện gọi `onScrub(e)` lần đầu, dòng ~863): thêm trước đó `scrubPrev.current = null; audioEngine.stopAll();`.

Lưu ý kiểu: `fireEventAudio` nhận state object — truyền spread với speed 1 như trên (đơn giản, không đổi chữ ký).

- [ ] **Step 6: Nút mute trong transport** — cạnh nút Play (sau nút ⏵ Next frame), thêm:

```tsx
<button
  title="Mute event audio"
  onClick={() => {
    audioEngine.setMuted(!audioMuted);
    setAudioMuted(!audioMuted);
  }}
>
  {audioMuted ? '🔇' : '🔊'}
</button>
```

State đầu component: `const [audioMuted, setAudioMuted] = useState(false);`

Đồng thời trong nút Play/Pause hiện có (onClick `setPlaying(!anim.playing)`) — khi pause: thêm `if (anim.playing) audioEngine.stopAll();` trước setPlaying.

- [ ] **Step 7: Verify** — `pnpm typecheck` + build; manual dev: import wav, tạo event có audio, key nó trong animation, Play nghe tiếng + thấy waveform, kéo playhead qua key nghe tiếng, mute im.

- [ ] **Step 8: Commit**

```bash
cd /Users/tuananh/Projects/you/spine_editor/client
pnpm exec prettier --write packages/editor/src/components/EventWave.tsx packages/editor/src/components/TimelinePanel.tsx packages/editor/src/styles.css
pnpm lint
cd .. && git add client/packages/editor
git commit -m "P20: dopesheet waveform + event audio on play/scrub + mute toggle

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: MCP import_audio (62 tools) + bridge e2e

**Files:**

- Modify: `client/packages/shared/src/index.ts` (BRIDGE_OPS + 'import_audio')
- Modify: `client/packages/shared/src/tools.ts` (def import_audio; comment 61 → 62)
- Modify: `client/packages/shared/test/tools.test.ts` (61 → 62)
- Modify: `client/packages/editor/src/bridge/ops.ts` (case import_audio + get_project_state.audioAssets)
- Modify: `client/packages/mcp-server/e2e/bridge.mjs` (WAV inline + bước audio + summary)

**Interfaces:**

- Consumes: store `addAudioAssets`/`audioAssets`, `audioEngine`, `uniqueName`.

- [ ] **Step 1: shared/index.ts** — trong BRIDGE_OPS, sau `'adjust_weights',` thêm `'import_audio',`.

- [ ] **Step 2: shared/tools.ts** — sau def `import_image` thêm:

```ts
def(
  'import_audio',
  'Import an audio clip (data URL) as a project audio asset. Reference it from an event via set_event { audio: <asset> } — the editor then plays it during playback/scrub and shows its waveform in the dopesheet.',
  {
    name: z.string().describe('Asset name without extension, e.g. "footstep".'),
    dataUrl: z.string().describe('data:audio/wav;base64,... payload.'),
  },
),
```

Đổi comment đầu file `the 61 editor tools` → `the 62 editor tools`. Test: `toHaveLength(61)`/`size).toBe(61)` → 62 (3 chỗ trong tools.test.ts).

- [ ] **Step 3: ops.ts** — case mới sau `import_image` case (tìm `case 'import_image'`); import thêm `audioEngine` từ `'../audio/engine.js'`:

```ts
case 'import_audio': {
  const s = state();
  const dataUrl = str(params, 'dataUrl');
  if (!dataUrl.startsWith('data:audio/')) {
    throw new Error('Param "dataUrl" must be a data:audio/... URL.');
  }
  const name = uniqueName(str(params, 'name'), (n) => n in s.audioAssets);
  s.addAudioAssets([{ name, dataUrl }]);
  audioEngine.ensure(name, dataUrl);
  return { asset: name };
}
```

Trong `case 'get_project_state'` — thêm vào object trả về (sau `assets: ...`):

```ts
audioAssets: Object.keys(s.audioAssets),
```

- [ ] **Step 4: suites** — `pnpm test` từ client/: shared 3 xanh với 62; core/mcp-server không đổi. `pnpm typecheck`.

- [ ] **Step 5: bridge.mjs — bước audio** — sau block 9c2 (P19 mesh, tìm `const p19Flag`) thêm:

```js
// 9c3. Audio asset + event audio (Phase 20).
function tinyWavDataUrl() {
  const rate = 8000;
  const samples = Math.floor(rate * 0.2);
  const buf = Buffer.alloc(44 + samples * 2);
  buf.write('RIFF', 0);
  buf.writeUInt32LE(36 + samples * 2, 4);
  buf.write('WAVEfmt ', 8);
  buf.writeUInt32LE(16, 16);
  buf.writeUInt16LE(1, 20); // PCM
  buf.writeUInt16LE(1, 22); // mono
  buf.writeUInt32LE(rate, 24);
  buf.writeUInt32LE(rate * 2, 28);
  buf.writeUInt16LE(2, 32);
  buf.writeUInt16LE(16, 34);
  buf.write('data', 36);
  buf.writeUInt32LE(samples * 2, 40);
  for (let i = 0; i < samples; i++) {
    buf.writeInt16LE(Math.round(Math.sin((i / rate) * 2 * Math.PI * 440) * 12000), 44 + i * 2);
  }
  return `data:audio/wav;base64,${buf.toString('base64')}`;
}
const audioImport = await call('import_audio', { name: 'clang-sfx', dataUrl: tinyWavDataUrl() });
await call('set_event', { name: 'clang', audio: audioImport.asset, volume: 0.9 });
await call('set_event_keyframe', { animation: 'walk', name: 'clang', time: 0.25, balance: -0.5 });
const audioState = await call('get_project_state');
const audioExport = JSON.parse((await call('export_spine_json')).json);
```

Summary thêm (cạnh `meshEditWorks`):

```js
audioWorks:
  audioImport.asset === 'clang-sfx' &&
  (audioState.audioAssets ?? []).includes('clang-sfx') &&
  audioExport.events?.clang?.audio === 'clang-sfx' &&
  (audioExport.animations?.walk?.events ?? []).some(
    (k) => k.name === 'clang' && k.balance === -0.5,
  ),
```

- [ ] **Step 6: Commit**

```bash
cd /Users/tuananh/Projects/you/spine_editor/client
pnpm exec prettier --write packages/shared/src packages/shared/test packages/editor/src/bridge/ops.ts packages/mcp-server/e2e/bridge.mjs
pnpm lint
cd .. && git add client/packages/shared client/packages/editor client/packages/mcp-server
git commit -m "P20: MCP import_audio — 62 tools

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: E2E battery + docs

**Files:** Modify `CLAUDE.md`, `PLAN.md`.

- [ ] **Step 1: Battery** (thứ tự chuẩn):

1. `cd client && pnpm build` sạch; kill :4173; từ `client/packages/editor`: `pnpm exec vite preview --port 4173` (nền).
2. `CHROMIUM_PATH="$HOME/Library/Caches/ms-playwright/chromium-1228/chrome-mac-arm64/Google Chrome for Testing.app/Contents/MacOS/Google Chrome for Testing" node e2e/smoke.mjs` → `"issues": []`.
3. `node e2e/anim.mjs` → `"issues": []`.
4. Kill :8017; từ `client/packages/mcp-server`: `node e2e/bridge.mjs` → `toolCount: 62`, `meshEditWorks: true`, **`audioWorks: true`**, setIk/rigFromParts/presetWalk true.
5. Server fake (data dir mới, `SPINE_SERVER_CHAT_FAKE=1 SPINE_SERVER_SEGMENT_FAKE=1`, :8100) → `node e2e/chat.mjs` → `chatRigWorks: true`.
6. `cd server && uv run pytest -q` → 60 passed, 2 skipped.
7. Kill server nền.

- [ ] **Step 2: Docs**

- CLAUDE.md: sau câu Phase 19, thêm: `**Phase 20 done**: audio — 'AudioAsset' trong store + project payload (field optional 'audioAssets'), Web Audio engine ('src/audio/engine.ts': decode cache, peaks, play qua gain+panner, mute), section AUDIO trong tree (import/preview/xóa), EventDock audio thành select, waveform theo zoom trên track events ('EventWave'), phát tiếng event khi play (loop-wrap aware, volume/balance = key ?? def, rate = speed) và khi scrub 2 chiều, nút 🔊/🔇 transport; MCP 'import_audio' — **62 MCP tools total**.` — đổi đuôi `Next: PLAN.md §8 phases 20–22.` → `Next: PLAN.md §8 phases 21–22.`
- PLAN.md row 20: `| **20** | Audio view — ✅ (07/2026) | ...` (giữ mô tả).

- [ ] **Step 3: Format + final** — prettier CLAUDE.md/PLAN.md (config client), `pnpm format:check`, commit:

```bash
cd /Users/tuananh/Projects/you/spine_editor
git add CLAUDE.md PLAN.md
git commit -m "P20: e2e + docs — Phase 20 complete

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Final acceptance (spec §8)

- [ ] Suites xanh (shared 62), pytest không đổi, typecheck/lint/format sạch
- [ ] 4 e2e xanh: `toolCount: 62`, `audioWorks: true`, không selector cũ nào đổi
- [ ] Manual: import wav → AUDIO row + ▶ kêu; event audio select; waveform hiện đúng vị trí key và scale theo zoom; Play/scrub có tiếng, volume/balance đúng, mute im, pause dừng tiếng
- [ ] Project save/open giữ audio; project cũ (không có audioAssets) mở bình thường
- [ ] Docs cập nhật (62 tools, row 20 ✅)
