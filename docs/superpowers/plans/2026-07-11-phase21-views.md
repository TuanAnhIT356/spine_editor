# Phase 21 — Views phụ + polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Slot Color view + tint black end-to-end (validate/evaluate/render), Metrics/Settings windows, Welcome screen, hotkeys B/N/G/C/V/X/Z, MCP `set_slot_color` (63 tools).

**Architecture:** Core mở rộng validation SetSlotProperties + evaluate rgba2/rgb2; renderer áp tint black bằng ColorMatrixFilter (chỉ gắn khi slot có dark). Editor thêm 3 floating window (pattern sẵn có) + Welcome overlay + store `settings` persist localStorage. MCP thêm 1 tool + mở rộng keyframe tool.

**Tech Stack:** TypeScript strict, Vitest, React, zustand, PixiJS v8 ColorMatrixFilter (không dependency mới).

## Global Constraints

- Spec: `docs/superpowers/specs/2026-07-11-phase21-views-design.md`.
- pnpm từ `client/`; test 1 file: `pnpm --filter @spine-editor/core test -- test/<file>.test.ts`.
- KHÔNG dependency mới; KHÔNG đổi selector/text e2e cũ ("Edit","Done","New","Play","Pause",`.tree`,`.row.bone`,`.ruler`,`.track .key`,`.timeline-header`).
- Hotkey guard PHẢI chặn cả textarea/contentEditable (chat e2e gõ chữ vào textarea — thiếu guard là hotkey chữ cái phá chat).
- Welcome chỉ hiện khi không có autosave VÀ `settings.welcome`; 5 file e2e (smoke/anim/chat/server + bridge) thêm addInitScript tắt welcome TRƯỚC page.goto.
- Commit message kết thúc `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Prettier trước mỗi commit; `pnpm lint` sạch.

---

### Task 1: Core — slot color/dark validate + rgba2/rgb2 evaluate + keyframe dark

**Files:**

- Modify: `client/packages/core/src/commands/slots.ts` (validate color/dark trong SetSlotProperties)
- Modify: `client/packages/core/src/evaluate.ts` (rgba2/rgb2 + computeAnimatedDarkColors)
- Modify: `client/packages/core/src/commands/animations.ts` (UpsertSlotColorKeyframe nhận dark → rgba2)
- Test: `client/packages/core/test/slot-color.test.ts` (mới)

**Interfaces:**

- Consumes: `SlotPatch = Partial<Omit<SlotData,'name'>>` (đã gồm `color: string`, `dark: string | null`); `parseHex`/`hexByte`/`segmentValue`/`sampleColorTimeline` (nội bộ evaluate.ts); `SpineTwoColorKey {time?, light?, dark?, curve?}`.
- Produces:
  - `SetSlotProperties` validate: `color` phải `^[0-9a-fA-F]{8}$`, `dark` phải `^[0-9a-fA-F]{6}$` hoặc `null`.
  - `computeAnimatedDarkColors(data, animationName, time): Map<string, string>` (dark 6-hex; slot không có timeline dark → KHÔNG có entry — caller fallback setup).
  - `computeAnimatedColors` ưu tiên light từ `rgba2` khi timeline tồn tại (8-hex), `rgb2` (6-hex light + giữ alpha hiện hành).
  - `new UpsertSlotColorKeyframe(animation, slot, key, dark?)` — có `dark` (6-hex) → ghi key vào `timelines.rgba2` dạng `{time?, light: key.color, dark, curve?}`; không → `rgba` như cũ.

- [ ] **Step 1: Test fail** — `client/packages/core/test/slot-color.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import {
  SetSlotProperties,
  SpineDocument,
  UpsertSlotColorKeyframe,
  computeAnimatedColors,
  computeAnimatedDarkColors,
  createBone,
  createEmptySkeleton,
  createSlot,
  parseSpineJson,
  serializeSpineJson,
} from '../src/index.js';

function docWithSlot() {
  const data = createEmptySkeleton();
  data.bones.push(createBone('b', 'root'));
  data.slots.push(createSlot('s', 'b'));
  return new SpineDocument(data);
}

describe('SetSlotProperties color/dark', () => {
  it('sets color and dark, disables with null, undoes', () => {
    const doc = docWithSlot();
    doc.execute(new SetSlotProperties('s', { color: 'ff8800ff', dark: '332211' }));
    expect(doc.data.slots[0]!.color).toBe('ff8800ff');
    expect(doc.data.slots[0]!.dark).toBe('332211');
    doc.execute(new SetSlotProperties('s', { dark: null }));
    expect(doc.data.slots[0]!.dark).toBeNull();
    doc.undo();
    expect(doc.data.slots[0]!.dark).toBe('332211');
    doc.undo();
    expect(doc.data.slots[0]!.color).toBe('ffffffff');
    expect(doc.data.slots[0]!.dark).toBeNull();
  });

  it('rejects malformed hex', () => {
    const doc = docWithSlot();
    expect(() => doc.execute(new SetSlotProperties('s', { color: 'red' }))).toThrow(/8-digit/);
    expect(() => doc.execute(new SetSlotProperties('s', { dark: 'ff00' }))).toThrow(/6-digit/);
  });
});

describe('two-color timelines', () => {
  function dataWithRgba2() {
    const doc = docWithSlot();
    doc.data.animations['a'] = {
      slots: {
        s: {
          rgba2: [
            { light: 'ff0000ff', dark: '000000' },
            { time: 1, light: '00ff00ff', dark: 'ffffff' },
          ],
        },
      },
    };
    return doc.data;
  }

  it('samples dark colors at the midpoint', () => {
    const dark = computeAnimatedDarkColors(dataWithRgba2(), 'a', 0.5).get('s')!;
    const r = parseInt(dark.slice(0, 2), 16);
    expect(r).toBeGreaterThanOrEqual(127);
    expect(r).toBeLessThanOrEqual(129);
  });

  it('rgba2 light overrides the slot color in computeAnimatedColors', () => {
    const light = computeAnimatedColors(dataWithRgba2(), 'a', 0).get('s');
    expect(light).toBe('ff0000ff');
  });

  it('slots without two-color timelines have no dark entry', () => {
    expect(computeAnimatedDarkColors(dataWithRgba2(), 'a', 0).has('missing')).toBe(false);
  });
});

describe('UpsertSlotColorKeyframe with dark', () => {
  it('writes an rgba2 key and undoes', () => {
    const doc = docWithSlot();
    doc.data.animations['a'] = {};
    doc.execute(new UpsertSlotColorKeyframe('a', 's', { time: 0.5, color: 'ff0000ff' }, '112233'));
    const keys = doc.data.animations['a']!.slots?.['s']?.rgba2;
    expect(keys).toHaveLength(1);
    expect(keys![0]).toMatchObject({ time: 0.5, light: 'ff0000ff', dark: '112233' });
    expect(doc.data.animations['a']!.slots?.['s']?.rgba).toBeUndefined();
    doc.undo();
    expect(doc.data.animations['a']!.slots?.['s']?.rgba2).toBeUndefined();
  });

  it('rejects malformed dark', () => {
    const doc = docWithSlot();
    doc.data.animations['a'] = {};
    expect(() =>
      doc.execute(new UpsertSlotColorKeyframe('a', 's', { color: 'ff0000ff' }, 'xyz')),
    ).toThrow(/6-digit/);
  });
});

describe('round-trip', () => {
  it('keeps slot.dark and rgba2 keys through serialize/parse', () => {
    const doc = docWithSlot();
    doc.data.animations['flick'] = {};
    doc.execute(new SetSlotProperties('s', { color: 'ff8800ff', dark: '332211' }));
    doc.execute(new UpsertSlotColorKeyframe('flick', 's', { color: 'ffffffff' }, 'abcdef'));
    const json = serializeSpineJson(doc.data);
    const { data } = parseSpineJson(json);
    expect(data.slots[0]!.dark).toBe('332211');
    expect(data.animations['flick']!.slots?.['s']?.rgba2?.[0]).toMatchObject({
      light: 'ffffffff',
      dark: 'abcdef',
    });
  });
});
```

(Animation `flick` được tạo trước khi key — UpsertSlotColorKeyframe yêu cầu animation tồn tại.)

- [ ] **Step 2: RED** — `pnpm --filter @spine-editor/core test -- test/slot-color.test.ts` → FAIL (computeAnimatedDarkColors chưa export; validate chưa có).

- [ ] **Step 3: slots.ts** — trong `SetSlotProperties.execute`, sau check bone:

```ts
if (this.patch.color !== undefined && !/^[0-9a-fA-F]{8}$/.test(this.patch.color)) {
  throw new Error('Slot color must be 8-digit rgba hex, e.g. "ff8800ff".');
}
if (
  this.patch.dark !== undefined &&
  this.patch.dark !== null &&
  !/^[0-9a-fA-F]{6}$/.test(this.patch.dark)
) {
  throw new Error('Dark color must be 6-digit rgb hex, e.g. "332211" (or null to disable).');
}
```

- [ ] **Step 4: evaluate.ts** — (a) helper sau `sampleColorTimeline`:

```ts
/** Samples an rgba2/rgb2 timeline; returns concatenated channel bytes. */
function sampleTwoColor(
  keys: SpineTwoColorKey[],
  time: number,
  lightLen: number, // 4 cho rgba2, 3 cho rgb2
  setupLight: string,
  setupDark: string,
): { light: string; dark: string } {
  const lightOf = (k: SpineTwoColorKey) => (k.light ?? setupLight).slice(0, lightLen * 2);
  const darkOf = (k: SpineTwoColorKey) => k.dark ?? setupDark;
  const first = keys[0];
  if (!first) return { light: setupLight.slice(0, lightLen * 2), dark: setupDark };
  const pick = (k: SpineTwoColorKey) => ({ light: lightOf(k), dark: darkOf(k) });
  if (time <= (first.time ?? 0)) return pick(first);
  let i = 0;
  while (i < keys.length - 1 && (keys[i + 1]?.time ?? 0) <= time) i++;
  const k1 = keys[i]!;
  if (i === keys.length - 1) return pick(k1);
  const k2 = keys[i + 1]!;
  const c1 = [...parseHex(lightOf(k1)), ...parseHex(darkOf(k1))];
  const c2 = [...parseHex(lightOf(k2)), ...parseHex(darkOf(k2))];
  const t1 = k1.time ?? 0;
  const t2 = k2.time ?? 0;
  const out = c1.map((v, ch) => segmentValue(time, t1, v, t2, c2[ch] ?? v, k1.curve, ch));
  const bytes = out.map(hexByte);
  return {
    light: bytes.slice(0, lightLen).join(''),
    dark: bytes.slice(lightLen).join(''),
  };
}
```

(import type `SpineTwoColorKey` từ spine-json/types nếu chưa có trong file; `parseHex` trả mảng số per byte — xác nhận chữ ký thật trong file, chỉnh cho khớp.)

(b) Trong `computeAnimatedColors`, đổi điều kiện skip và thêm rgba2/rgb2 (đặt TRƯỚC alpha để alpha timeline vẫn override):

```ts
if (!timelines?.rgba && !timelines?.alpha && !timelines?.rgba2 && !timelines?.rgb2) continue;
let color = slot.color;
if (timelines.rgba) color = sampleColorTimeline(timelines.rgba, time, slot.color);
if (timelines.rgba2?.length) {
  color = sampleTwoColor(timelines.rgba2, time, 4, slot.color, slot.dark ?? '000000').light;
}
if (timelines.rgb2?.length) {
  const lightRgb = sampleTwoColor(timelines.rgb2, time, 3, slot.color, slot.dark ?? '000000').light;
  color = lightRgb + color.slice(6, 8);
}
```

(c) Hàm mới export sau `computeAnimatedColors`:

```ts
/** Dark (tint-black) color per slot from rgba2/rgb2 timelines at time t. */
export function computeAnimatedDarkColors(
  data: SkeletonData,
  animationName: string,
  time: number,
): Map<string, string> {
  const out = new Map<string, string>();
  const anim = data.animations[animationName];
  if (!anim?.slots) return out;
  for (const slot of data.slots) {
    const timelines = anim.slots[slot.name];
    const keys = timelines?.rgba2?.length ? timelines.rgba2 : timelines?.rgb2;
    if (!keys?.length) continue;
    const lightLen = timelines?.rgba2?.length ? 4 : 3;
    out.set(
      slot.name,
      sampleTwoColor(keys, time, lightLen, slot.color, slot.dark ?? '000000').dark,
    );
  }
  return out;
}
```

(d) Header: trong danh sách "Not evaluated", xóa cụm `two-color (rgba2/rgb2) and` (sequence giữ lại).

- [ ] **Step 5: animations.ts** — `UpsertSlotColorKeyframe` constructor thêm `private readonly dark?: string`; execute: sau validate color hiện có, thêm:

```ts
if (this.dark !== undefined && !/^[0-9a-fA-F]{6}$/.test(this.dark)) {
  throw new Error('Dark must be 6-digit rgb hex, e.g. "332211".');
}
```

và thay block ghi key:

```ts
this.before = structuredClone(anim);
const slots = (anim.slots ??= {});
const timelines = (slots[this.slot] ??= {});
const time = this.key.time ?? 0;
if (this.dark !== undefined) {
  const keys = (timelines.rgba2 ??= []);
  const twoKey: SpineTwoColorKey = { light: this.key.color, dark: this.dark };
  if (time > 0) twoKey.time = time;
  if (this.key.curve !== undefined) twoKey.curve = this.key.curve;
  const existing = keys.findIndex((k) => Math.abs((k.time ?? 0) - time) < TIME_EPSILON);
  if (existing >= 0) keys[existing] = twoKey;
  else {
    keys.push(twoKey);
    keys.sort((a, b) => (a.time ?? 0) - (b.time ?? 0));
  }
} else {
  const keys = (timelines.rgba ??= []);
  const existing = keys.findIndex((k) => Math.abs((k.time ?? 0) - time) < TIME_EPSILON);
  if (existing >= 0) keys[existing] = this.key;
  else {
    keys.push(this.key);
    keys.sort((a, b) => (a.time ?? 0) - (b.time ?? 0));
  }
}
```

(import `SpineTwoColorKey` vào animations.ts.) Label đổi thành `Key color on "${slot}"` giữ nguyên.

- [ ] **Step 6: GREEN** — file test → PASS (xóa test scaffold rỗng); full core suite (`pnpm --filter @spine-editor/core test`) + `pnpm typecheck`.

- [ ] **Step 7: Commit**

```bash
cd /Users/tuananh/Projects/you/spine_editor/client
pnpm exec prettier --write packages/core/src/commands/slots.ts packages/core/src/commands/animations.ts packages/core/src/evaluate.ts packages/core/test/slot-color.test.ts
pnpm lint
cd .. && git add client/packages/core
git commit -m "P21: slot color/dark validation + rgba2/rgb2 evaluation + two-color keyframes

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: Renderer tint black + Viewport slotDarks

**Files:**

- Modify: `client/packages/editor/src/viewport/renderer.ts`
- Modify: `client/packages/editor/src/components/Viewport.tsx`

**Interfaces:**

- Consumes: `computeAnimatedDarkColors` (Task 1); pixi `ColorMatrixFilter` (import từ `pixi.js`).
- Produces: `RenderInput.slotDarks?: ReadonlyMap<string, string>`.

- [ ] **Step 1: RenderInput** — thêm sau `slotColors?`:

```ts
/** Animated dark (tint-black) colors per slot (animate mode). */
slotDarks?: ReadonlyMap<string, string>;
```

- [ ] **Step 2: renderer.ts** — import `ColorMatrixFilter` từ 'pixi.js'; field mới cạnh các cache: `private darkFilters = new Map<string, ColorMatrixFilter>();`. Helper cấp module (cạnh `tintOf`):

```ts
/** Spine two-color tint: out = tex×light + (1−tex)×dark, as a color matrix. */
function twoColorMatrix(light: string, dark: string): number[] {
  const l = [0, 1, 2].map((i) => parseInt(light.slice(i * 2, i * 2 + 2), 16) / 255);
  const d = [0, 1, 2].map((i) => parseInt(dark.slice(i * 2, i * 2 + 2), 16) / 255);
  // prettier-ignore
  return [
    l[0]! - d[0]!, 0, 0, 0, d[0]!,
    0, l[1]! - d[1]!, 0, 0, d[1]!,
    0, 0, l[2]! - d[2]!, 0, d[2]!,
    0, 0, 0, 1, 0,
  ];
}
```

Trong vòng render slot, thay 2 chỗ áp tint (mesh và sprite) bằng logic chung — ngay sau `const { tint, alpha } = tintOf(animColor);` thêm:

```ts
const dark = input.slotDarks?.get(slot.name) ?? slot.dark;
```

Với **mesh** (sau `mesh.tint = tint; mesh.alpha = alpha;`) và **sprite** (sau `sprite.tint = tint;` — trước dòng alpha) áp dụng cùng mẫu (viết helper method trong class để không lặp):

```ts
private applyDark(target: MeshSimple | Sprite, slotName: string, light: string, dark: string | null | undefined): void {
  if (dark) {
    let filter = this.darkFilters.get(slotName);
    if (!filter) {
      filter = new ColorMatrixFilter();
      this.darkFilters.set(slotName, filter);
    }
    filter.matrix = twoColorMatrix(light, dark) as ColorMatrixFilter['matrix'];
    target.filters = [filter];
    target.tint = 0xffffff;
  } else if (this.darkFilters.has(slotName)) {
    target.filters = [];
    this.darkFilters.delete(slotName);
  }
}
```

Call sites: mesh — `this.applyDark(mesh, slot.name, animColor, dark);` đặt SAU `mesh.tint = tint;` (applyDark ghi đè tint khi dark). Sprite — `this.applyDark(sprite, slot.name, animColor, dark);` SAU `sprite.tint = tint;`. `destroy()`: thêm `this.darkFilters.clear();`.

Lưu ý kiểu matrix: pixi v8 `ColorMatrixFilter.matrix` là `ColorMatrix` (mảng 20 số) — nếu TS phàn nàn, dùng `filter.matrix = twoColorMatrix(light, dark) as unknown as typeof filter.matrix;`.

- [ ] **Step 3: Viewport.tsx** — import `computeAnimatedDarkColors`; trong `buildRenderInput`, cạnh `slotColors`/`colors` (dòng gọi computeAnimatedColors — tìm `colors:` hoặc `slotColors:`):

```ts
slotDarks: animating
  ? computeAnimatedDarkColors(state.doc.data, state.anim.current!, state.anim.time)
  : undefined,
```

- [ ] **Step 4: Verify** — `pnpm typecheck` + `pnpm --filter @spine-editor/editor build`. Manual (tùy chọn): set dark qua console/bridge, thấy vùng tối nhuộm màu.

- [ ] **Step 5: Commit**

```bash
cd /Users/tuananh/Projects/you/spine_editor/client
pnpm exec prettier --write packages/editor/src/viewport/renderer.ts packages/editor/src/components/Viewport.tsx
pnpm lint
cd .. && git add client/packages/editor
git commit -m "P21: tint-black rendering via per-slot ColorMatrixFilter

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: Settings store + window + FPS/autosave wiring + hotkeys

**Files:**

- Modify: `client/packages/editor/src/state/store.ts` (settings + setSettings + stepFrame fps)
- Create: `client/packages/editor/src/components/SettingsWindow.tsx`
- Modify: `client/packages/editor/src/App.tsx` (hotkeys + autosave gate)
- Modify: `client/packages/editor/src/components/TimelinePanel.tsx` (fps thay 30)
- Modify: `client/packages/editor/src/components/Viewport.tsx` (ghost spacing fps)
- Modify: `client/packages/editor/src/shortcuts.ts` (+7 dòng)
- Modify: `client/packages/editor/src/components/Toolbar.tsx` (Views ▾ item Settings + render)
- Modify: `client/packages/editor/src/styles.css`

**Interfaces:**

- Produces: store `settings: EditorSettings` với `interface EditorSettings { fps: 24 | 30 | 60; autosave: boolean; welcome: boolean }`, action `setSettings(patch: Partial<EditorSettings>)` (persist localStorage key `spine-editor.settings`). Task 5 (Welcome) dùng `settings.welcome`.

- [ ] **Step 1: store.ts** — trên store:

```ts
export interface EditorSettings {
  fps: 24 | 30 | 60;
  autosave: boolean;
  welcome: boolean;
}

const SETTINGS_KEY = 'spine-editor.settings';

function loadSettings(): EditorSettings {
  const defaults: EditorSettings = { fps: 30, autosave: true, welcome: true };
  try {
    const raw = localStorage.getItem(SETTINGS_KEY);
    if (!raw) return defaults;
    const parsed = JSON.parse(raw) as Partial<EditorSettings>;
    return {
      fps: parsed.fps === 24 || parsed.fps === 60 ? parsed.fps : 30,
      autosave: parsed.autosave !== false,
      welcome: parsed.welcome !== false,
    };
  } catch {
    return defaults;
  }
}
```

Field `settings: EditorSettings;` + khai báo `setSettings(patch: Partial<EditorSettings>): void;`; initial `settings: loadSettings(),`; impl:

```ts
setSettings: (patch) =>
  set((s) => {
    const settings = { ...s.settings, ...patch };
    try {
      localStorage.setItem(SETTINGS_KEY, JSON.stringify(settings));
    } catch {
      /* quota/no-storage: keep in-memory */
    }
    return { settings };
  }),
```

`stepFrame`: `const step = 1 / 30;` → `const step = 1 / s.settings.fps;`

- [ ] **Step 2: TimelinePanel fps** — thêm selector `const fps = useEditor((s) => s.settings.fps);` đầu component; thay đúng các chỗ sau (giữ nguyên logic): dòng ~569 `anim.time * 30` → `* fps`; ~637 `frames / 30` → `/ fps`; ~658 `frames / 30` → `/ fps`; ~768 `Number(e.target.value) / 30` → `/ fps`; ~775 `anim.loopStart * 30` → `* fps`; ~781 `/ 30` → `/ fps`; ~791 `anim.loopEnd * 30` → `* fps`; và dòng `/ 30` tương ứng của loopEnd. (Grep `\b30\b` trong file để không sót — CHỈ các chỗ frame↔giây, KHÔNG đụng số 30 khác nếu có.)

- [ ] **Step 3: Viewport ghost fps** — dòng `const spacing = Math.max(ghostConfig.spacingFrames, 1) / 30;` → `/ state.ghostConfig ...` giữ nguyên biến, đổi mẫu số: `/ useEditor.getState().settings.fps` — nhưng hàm đã có `state` (getState) cục bộ: dùng `state.settings.fps`.

- [ ] **Step 4: App.tsx hotkeys** — (a) mở rộng guard hiện có:

```ts
const target = e.target as HTMLElement;
if (
  target instanceof HTMLInputElement ||
  target instanceof HTMLSelectElement ||
  target instanceof HTMLTextAreaElement ||
  target.isContentEditable
)
  return;
```

(b) sau nhánh `e.key === '6'`, thêm (import `bridgeRuntime` từ `./bridge/runtime.js` nếu chưa có — App có thể chưa import):

```ts
else if (e.key.toLowerCase() === 'v' && !mod) s.setTool('select');
else if (e.key.toLowerCase() === 'c' && !mod) s.setTool('create');
else if (e.key.toLowerCase() === 'b' && !mod) s.toggleViewFilter('bones', 'visible');
else if (e.key.toLowerCase() === 'n' && !mod) s.toggleViewFilter('bones', 'labels');
else if (e.key.toLowerCase() === 'g' && !mod && s.mode === 'animate') s.setGhost(!s.anim.ghost);
else if (e.key.toLowerCase() === 'x' && !mod) {
  const order = ['local', 'parent', 'world'] as const;
  s.setAxesMode(order[(order.indexOf(s.axesMode) + 1) % order.length]!);
} else if (e.key.toLowerCase() === 'z' && !mod) {
  bridgeRuntime.renderer?.setZoomCenter(1);
}
```

LƯU Ý: nhánh mod+z (undo) đứng TRƯỚC trong chuỗi if/else hiện có nên `z` thường không đụng undo (có `!mod` để chắc). Kiểm tra `toggleViewFilter(group, key)` + `setAxesMode` + `setGhost` là action thật trong store (đều có — grep nếu cần).

(c) `shortcuts.ts` thêm trước dòng `?`:

```ts
{ keys: 'V / C', description: 'Select tool / Create tool' },
{ keys: 'B', description: 'Toggle bone visibility' },
{ keys: 'N', description: 'Toggle bone name labels' },
{ keys: 'G', description: 'Toggle ghosting (animate)' },
{ keys: 'X', description: 'Cycle transform axes: Local → Parent → World' },
{ keys: 'Z', description: 'Reset viewport zoom to 100%' },
```

(d) Autosave gate — trong subscribe autosave (App.tsx ~90): đầu callback timer, bọc điều kiện:

```ts
timer = window.setTimeout(() => {
  const s = useEditor.getState();
  if (!s.settings.autosave) return;
  void saveAutosave({ ... như cũ ... }).catch(() => undefined);
}, 800);
```

Cập nhật dòng `← / →` trong SHORTCUTS: `'Step one frame back / forward (30fps)'` → `'Step one frame back / forward (Settings → FPS)'`.

- [ ] **Step 5: SettingsWindow.tsx** (mới — pattern floating chuẩn):

```tsx
import { useEffect, useRef, useState } from 'react';
import { useEditor } from '../state/store.js';

const POS_KEY = 'spine-editor.settings-window';

/** Editor preferences: frame rate, local autosave, welcome screen. */
export function SettingsWindow({ onClose }: { onClose: () => void }) {
  const settings = useEditor((s) => s.settings);
  const setSettings = useEditor((s) => s.setSettings);
  const [pos, setPos] = useState(() => {
    try {
      const saved = JSON.parse(localStorage.getItem(POS_KEY) ?? '') as { x: number; y: number };
      if (typeof saved.x === 'number') return saved;
    } catch {
      /* first open */
    }
    return { x: 120, y: 120 };
  });
  const drag = useRef<{ dx: number; dy: number } | null>(null);
  useEffect(() => localStorage.setItem(POS_KEY, JSON.stringify(pos)), [pos]);

  return (
    <div className="settings-window" style={{ left: pos.x, top: pos.y }}>
      <div
        className="chat-header"
        onPointerDown={(e) => {
          drag.current = { dx: e.clientX - pos.x, dy: e.clientY - pos.y };
          (e.target as HTMLElement).setPointerCapture(e.pointerId);
        }}
        onPointerMove={(e) => {
          if (!drag.current) return;
          const { dx, dy } = drag.current;
          setPos({ x: Math.max(0, e.clientX - dx), y: Math.max(0, e.clientY - dy) });
        }}
        onPointerUp={() => {
          drag.current = null;
        }}
      >
        <span className="chat-title">Settings</span>
        <button className="close" onPointerDown={(e) => e.stopPropagation()} onClick={onClose}>
          ×
        </button>
      </div>
      <div className="settings-body">
        <label className="field">
          <span>Frame rate</span>
          <select
            value={settings.fps}
            onChange={(e) => setSettings({ fps: Number(e.target.value) as 24 | 30 | 60 })}
          >
            {[24, 30, 60].map((f) => (
              <option key={f} value={f}>
                {f} fps
              </option>
            ))}
          </select>
        </label>
        <label className="field">
          <span>Autosave (local)</span>
          <input
            type="checkbox"
            checked={settings.autosave}
            onChange={(e) => setSettings({ autosave: e.target.checked })}
          />
        </label>
        <label className="field">
          <span>Show welcome on startup</span>
          <input
            type="checkbox"
            checked={settings.welcome}
            onChange={(e) => setSettings({ welcome: e.target.checked })}
          />
        </label>
      </div>
    </div>
  );
}
```

- [ ] **Step 6: Toolbar** — import SettingsWindow; state `const [showSettings, setShowSettings] = useState(false);`; Views ▾ thêm item (sau Weights):

```tsx
<label className="views-item">
  <input type="checkbox" checked={showSettings} onChange={() => setShowSettings((v) => !v)} />
  Settings
</label>
```

render: `{showSettings && <SettingsWindow onClose={() => setShowSettings(false)} />}`.

- [ ] **Step 7: styles.css** — cuối file:

```css
/* ---- Settings window (P21) ---- */
.settings-window {
  position: fixed;
  z-index: 25;
  width: 260px;
  display: flex;
  flex-direction: column;
  background: var(--panel-2);
  border: 1px solid var(--border);
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 8px 30px rgba(0, 0, 0, 0.5);
}
.settings-body {
  padding: 10px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.settings-body .field {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
  font-size: 12px;
}
```

- [ ] **Step 8: Verify + Commit** — `pnpm typecheck`, `pnpm --filter @spine-editor/editor build`, prettier các file, `pnpm lint`; commit:

```bash
cd /Users/tuananh/Projects/you/spine_editor && git add client/packages/editor
git commit -m "P21: Settings window (fps/autosave/welcome) + Spine-style letter hotkeys

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: ColorWindow + MetricsWindow + SlotDock button + Views items

**Files:**

- Create: `client/packages/editor/src/components/ColorWindow.tsx`
- Create: `client/packages/editor/src/components/MetricsWindow.tsx`
- Modify: `client/packages/editor/src/components/tree/dock/SlotDock.tsx` (nút "Color…")
- Modify: `client/packages/editor/src/components/Toolbar.tsx` (2 Views items + render)
- Modify: `client/packages/editor/src/styles.css`

**Interfaces:**

- Consumes: `SetSlotProperties` với color/dark (Task 1); store selection (`kind === 'slot'`), `mode`.

- [ ] **Step 1: ColorWindow.tsx**:

```tsx
import { SetSlotProperties } from '@spine-editor/core';
import { useEffect, useRef, useState } from 'react';
import { primarySelection, useEditor } from '../state/store.js';

const POS_KEY = 'spine-editor.color-window';

const toHex2 = (n: number) => n.toString(16).padStart(2, '0');

/** Spine-style Color view: setup color + optional tint black for the selected slot. */
export function ColorWindow({ onClose }: { onClose: () => void }) {
  const revision = useEditor((s) => s.revision);
  void revision;
  const doc = useEditor((s) => s.doc);
  const selection = useEditor((s) => s.selection);
  const mode = useEditor((s) => s.mode);
  const [pos, setPos] = useState(() => {
    try {
      const saved = JSON.parse(localStorage.getItem(POS_KEY) ?? '') as { x: number; y: number };
      if (typeof saved.x === 'number') return saved;
    } catch {
      /* first open */
    }
    return { x: 100, y: 100 };
  });
  const drag = useRef<{ dx: number; dy: number } | null>(null);
  useEffect(() => localStorage.setItem(POS_KEY, JSON.stringify(pos)), [pos]);

  const primary = primarySelection(selection);
  const slot = primary?.kind === 'slot' ? doc.findSlot(primary.name) : null;
  const animate = mode === 'animate';
  const patch = (p: { color?: string; dark?: string | null }) => {
    if (slot) useEditor.getState().execute(new SetSlotProperties(slot.name, p));
  };

  const light = slot?.color ?? 'ffffffff';
  const alpha = parseInt(light.slice(6, 8), 16);

  return (
    <div className="color-window" style={{ left: pos.x, top: pos.y }}>
      <div
        className="chat-header"
        onPointerDown={(e) => {
          drag.current = { dx: e.clientX - pos.x, dy: e.clientY - pos.y };
          (e.target as HTMLElement).setPointerCapture(e.pointerId);
        }}
        onPointerMove={(e) => {
          if (!drag.current) return;
          const { dx, dy } = drag.current;
          setPos({ x: Math.max(0, e.clientX - dx), y: Math.max(0, e.clientY - dy) });
        }}
        onPointerUp={() => {
          drag.current = null;
        }}
      >
        <span className="chat-title">Color{slot ? `: ${slot.name}` : ''}</span>
        <button className="close" onPointerDown={(e) => e.stopPropagation()} onClick={onClose}>
          ×
        </button>
      </div>
      <div className="color-body">
        {!slot ? (
          <div className="empty">Select a slot in the tree.</div>
        ) : (
          <>
            {animate && (
              <div className="empty">
                Setup colors only — key colors via the dopesheet or set_slot_color_keyframe.
              </div>
            )}
            <label className="field">
              <span>Color</span>
              <input
                type="color"
                disabled={animate}
                value={`#${light.slice(0, 6)}`}
                onChange={(e) => patch({ color: e.target.value.slice(1) + light.slice(6, 8) })}
              />
            </label>
            <label className="field">
              <span>Alpha</span>
              <input
                type="range"
                min={0}
                max={255}
                disabled={animate}
                value={alpha}
                onChange={(e) =>
                  patch({ color: light.slice(0, 6) + toHex2(Number(e.target.value)) })
                }
              />
            </label>
            <label className="field">
              <span>Tint black</span>
              <input
                type="checkbox"
                disabled={animate}
                checked={slot.dark !== null}
                onChange={(e) => patch({ dark: e.target.checked ? '000000' : null })}
              />
            </label>
            {slot.dark !== null && (
              <label className="field">
                <span>Dark</span>
                <input
                  type="color"
                  disabled={animate}
                  value={`#${slot.dark}`}
                  onChange={(e) => patch({ dark: e.target.value.slice(1) })}
                />
              </label>
            )}
          </>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: MetricsWindow.tsx**:

```tsx
import { useEffect, useRef, useState } from 'react';
import { useEditor } from '../state/store.js';

const POS_KEY = 'spine-editor.metrics-window';

/** Document statistics, Spine's Metrics view equivalent. */
export function MetricsWindow({ onClose }: { onClose: () => void }) {
  const revision = useEditor((s) => s.revision);
  void revision;
  const doc = useEditor((s) => s.doc);
  const assets = useEditor((s) => s.assets);
  const audioAssets = useEditor((s) => s.audioAssets);
  const [pos, setPos] = useState(() => {
    try {
      const saved = JSON.parse(localStorage.getItem(POS_KEY) ?? '') as { x: number; y: number };
      if (typeof saved.x === 'number') return saved;
    } catch {
      /* first open */
    }
    return { x: 140, y: 100 };
  });
  const drag = useRef<{ dx: number; dy: number } | null>(null);
  useEffect(() => localStorage.setItem(POS_KEY, JSON.stringify(pos)), [pos]);

  const d = doc.data;
  const byType = new Map<string, number>();
  let meshVertices = 0;
  let meshTriangles = 0;
  for (const skin of d.skins) {
    for (const bySlot of Object.values(skin.attachments ?? {})) {
      for (const att of Object.values(bySlot)) {
        const type = att.type ?? 'region';
        byType.set(type, (byType.get(type) ?? 0) + 1);
        if (att.type === 'mesh') {
          meshVertices += att.uvs.length / 2;
          meshTriangles += att.triangles.length / 3;
        }
      }
    }
  }
  const rows: [string, number][] = [
    ['Bones', d.bones.length],
    ['Slots', d.slots.length],
    ['Skins', d.skins.length],
    ...[...byType.entries()].map(([t, n]) => [`Attachments: ${t}`, n] as [string, number]),
    ['IK constraints', d.ik.length],
    ['Transform constraints', d.transform.length],
    ['Path constraints', d.path?.length ?? 0],
    ['Physics constraints', d.physics?.length ?? 0],
    ['Events', Object.keys(d.events).length],
    ['Animations', Object.keys(d.animations).length],
    ['Images', Object.keys(assets).length],
    ['Audio', Object.keys(audioAssets).length],
    ['Mesh vertices', meshVertices],
    ['Mesh triangles', meshTriangles],
  ];

  return (
    <div className="metrics-window" style={{ left: pos.x, top: pos.y }}>
      <div
        className="chat-header"
        onPointerDown={(e) => {
          drag.current = { dx: e.clientX - pos.x, dy: e.clientY - pos.y };
          (e.target as HTMLElement).setPointerCapture(e.pointerId);
        }}
        onPointerMove={(e) => {
          if (!drag.current) return;
          const { dx, dy } = drag.current;
          setPos({ x: Math.max(0, e.clientX - dx), y: Math.max(0, e.clientY - dy) });
        }}
        onPointerUp={() => {
          drag.current = null;
        }}
      >
        <span className="chat-title">Metrics</span>
        <button className="close" onPointerDown={(e) => e.stopPropagation()} onClick={onClose}>
          ×
        </button>
      </div>
      <div className="metrics-body">
        <table>
          <tbody>
            {rows.map(([label, n]) => (
              <tr key={label}>
                <td>{label}</td>
                <td className="metrics-n">{n}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  );
}
```

KIỂM TRA tên field constraints trong `SkeletonData` (model/types.ts): `ik`, `transform`, `path`, `physics` — chỉnh optional chaining theo kiểu thật (nếu `path`/`physics` non-optional thì bỏ `?? 0`).

- [ ] **Step 3: SlotDock** — trong component slot dock chính (khu render các section — cạnh AttachmentsSection call), thêm nút mở Color window: đơn giản nhất là để ColorWindow toggle ở Toolbar; SlotDock chỉ cần nút phát event → dùng custom event: `window.dispatchEvent(new CustomEvent('spine-editor:open-color'))`; Toolbar lắng nghe event này để `setShowColor(true)`:

SlotDock (đầu AttachmentsSection return, sau panel-title Attachments — đặt ở khu slot properties chính nếu có; nếu SlotDock có section properties slot — tìm `BLEND_MODES` select — đặt nút cạnh đó):

```tsx
<button onClick={() => window.dispatchEvent(new CustomEvent('spine-editor:open-color'))}>
  Color…
</button>
```

Toolbar: trong useEffect mới:

```ts
useEffect(() => {
  const open = () => setShowColor(true);
  window.addEventListener('spine-editor:open-color', open);
  return () => window.removeEventListener('spine-editor:open-color', open);
}, []);
```

- [ ] **Step 4: Toolbar Views** — state `showColor`, `showMetrics`; 2 items sau Settings item (Task 3):

```tsx
<label className="views-item">
  <input type="checkbox" checked={showColor} onChange={() => setShowColor((v) => !v)} />
  Color
</label>
<label className="views-item">
  <input type="checkbox" checked={showMetrics} onChange={() => setShowMetrics((v) => !v)} />
  Metrics
</label>
```

render: `{showColor && <ColorWindow onClose={() => setShowColor(false)} />}` + tương tự Metrics.

- [ ] **Step 5: styles.css**:

```css
/* ---- Color + Metrics windows (P21) ---- */
.color-window,
.metrics-window {
  position: fixed;
  z-index: 25;
  width: 250px;
  display: flex;
  flex-direction: column;
  background: var(--panel-2);
  border: 1px solid var(--border);
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 8px 30px rgba(0, 0, 0, 0.5);
}
.color-body,
.metrics-body {
  padding: 10px;
  display: flex;
  flex-direction: column;
  gap: 8px;
  max-height: 420px;
  overflow-y: auto;
}
.color-body .field {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
  font-size: 12px;
}
.metrics-body table {
  width: 100%;
  border-collapse: collapse;
  font-size: 12px;
}
.metrics-body td {
  padding: 2px 4px;
  border-bottom: 1px solid var(--border);
}
.metrics-n {
  text-align: right;
  color: var(--text-dim);
}
```

- [ ] **Step 6: Verify + Commit** — typecheck/build/prettier/lint; commit `P21: Color + Metrics windows` (trailer chuẩn).

---

### Task 5: WelcomeScreen + e2e init

**Files:**

- Create: `client/packages/editor/src/components/WelcomeScreen.tsx`
- Modify: `client/packages/editor/src/state/actions.ts` (export openProjectFile/importSpineJsonFile)
- Modify: `client/packages/editor/src/components/Toolbar.tsx` (dùng lại 2 hàm)
- Modify: `client/packages/editor/src/App.tsx` (show welcome khi không autosave)
- Modify: `client/packages/editor/src/styles.css`
- Modify: `client/packages/editor/e2e/smoke.mjs`, `anim.mjs`, `chat.mjs`, `server.mjs`, `client/packages/mcp-server/e2e/bridge.mjs` (addInitScript)

**Interfaces:**

- Consumes: `settings.welcome` (Task 3), `listProjects`/`getProject`/`useServer` (server/api.ts), `replaceProject`.
- Produces: `openProjectFile(file: File): Promise<void>`, `importSpineJsonFile(file: File): Promise<void>` (actions.ts — throw Error khi file sai).

- [ ] **Step 1: actions.ts** — chuyển logic từ Toolbar:

```ts
import { readFileAsText } from './persistence.js'; // kiểm tra: hàm này ở persistence hay Toolbar — nếu ở Toolbar, chuyển sang actions.ts
import { parseSpineJson } from '@spine-editor/core'; // chỉ khi cần cho import
import type { ProjectPayload } from './persistence.js';

/** Opens a .spine-editor project file into the store (throws on invalid). */
export async function openProjectFile(file: File): Promise<void> {
  const payload = JSON.parse(await readFileAsText(file)) as ProjectPayload;
  if (payload.format !== 'spine-editor-project') throw new Error('Not a project file.');
  useEditor.getState().replaceProject(payload.spine, payload.assets, payload.audioAssets ?? []);
}

/** Imports a raw Spine JSON file, keeping current image/audio assets. */
export async function importSpineJsonFile(file: File): Promise<void> {
  const json = JSON.parse(await readFileAsText(file)) as import('@spine-editor/core').SpineJson;
  const state = useEditor.getState();
  state.replaceProject(json, Object.values(state.assets), Object.values(state.audioAssets));
}
```

KIỂM TRA vị trí `readFileAsText` (grep) — Toolbar đang dùng; nếu nó định nghĩa trong Toolbar.tsx thì move sang persistence.ts (export) và cập nhật import Toolbar. Toolbar.onOpenProject/onImportSpineJson đổi thành gọi 2 hàm này trong try/catch setError hiện có (giữ validate issues log của import nếu có — xem code thật, giữ hành vi cũ: nếu onImportSpineJson hiện trả issues → giữ trong hàm mới trả `ValidationIssue[]` và Toolbar hiển thị như cũ; chỉnh chữ ký theo code thật).

- [ ] **Step 2: WelcomeScreen.tsx**:

```tsx
import { useEffect, useRef, useState } from 'react';
import { getProject, listProjects, useServer, type ProjectSummary } from '../server/api.js';
import { openProjectFile, importSpineJsonFile } from '../state/actions.js';
import { useEditor } from '../state/store.js';

/** First-run overlay: create/open/import + server projects. */
export function WelcomeScreen({ onClose }: { onClose: () => void }) {
  const settings = useEditor((s) => s.settings);
  const setSettings = useEditor((s) => s.setSettings);
  const user = useServer((s) => s.user);
  const [projects, setProjects] = useState<ProjectSummary[] | null>(null);
  const openInput = useRef<HTMLInputElement | null>(null);
  const importInput = useRef<HTMLInputElement | null>(null);

  useEffect(() => {
    if (!user) return;
    void listProjects()
      .then(setProjects)
      .catch(() => setProjects([]));
  }, [user]);

  const run = (fn: () => Promise<void>) => {
    void fn()
      .then(onClose)
      .catch((err) =>
        useEditor.getState().setError(err instanceof Error ? err.message : String(err)),
      );
  };

  return (
    <div className="welcome-overlay">
      <div className="welcome-panel">
        <div className="welcome-title">Spine Editor</div>
        <div className="welcome-sub">Web-based 2D skeletal animation</div>
        <div className="welcome-actions">
          <button onClick={onClose}>New Project</button>
          <button onClick={() => openInput.current?.click()}>Open Project…</button>
          <button onClick={() => importInput.current?.click()}>Import Spine JSON…</button>
        </div>
        {user ? (
          <div className="welcome-projects">
            <div className="panel-title">Server projects</div>
            {projects === null && <div className="empty">Loading…</div>}
            {projects?.length === 0 && <div className="empty">No projects yet.</div>}
            {projects?.map((p) => (
              <button
                key={p.id}
                className="welcome-project"
                onClick={() =>
                  run(async () => {
                    const full = await getProject(p.id);
                    useEditor
                      .getState()
                      .replaceProject(
                        full.data.spine,
                        full.data.assets,
                        full.data.audioAssets ?? [],
                      );
                  })
                }
              >
                {p.name}
              </button>
            ))}
          </div>
        ) : (
          <div className="empty">Sign in (Server) to sync projects.</div>
        )}
        <label className="welcome-startup">
          <input
            type="checkbox"
            checked={settings.welcome}
            onChange={(e) => setSettings({ welcome: e.target.checked })}
          />
          Show on startup
        </label>
        <button className="close welcome-close" onClick={onClose}>
          ×
        </button>
        <input
          ref={openInput}
          type="file"
          accept="application/json,.json"
          hidden
          onChange={(e) => {
            const f = e.target.files?.[0];
            if (f) run(() => openProjectFile(f));
            e.target.value = '';
          }}
        />
        <input
          ref={importInput}
          type="file"
          accept="application/json,.json"
          hidden
          onChange={(e) => {
            const f = e.target.files?.[0];
            if (f) run(() => importSpineJsonFile(f));
            e.target.value = '';
          }}
        />
      </div>
    </div>
  );
}
```

(`useServer` selector `user`: kiểm tra field thật trong server/api.ts — có thể là `email`/`user`; chỉnh theo thật. `ProjectSummary` có `id`, `name` — xác nhận.)

- [ ] **Step 3: App.tsx** — state `const [showWelcome, setShowWelcome] = useState(false);`; trong effect loadAutosave:

```ts
void loadAutosave().then((payload) => {
  if (payload) {
    useEditor.getState().replaceProject(payload.spine, payload.assets, payload.audioAssets ?? []);
  } else if (useEditor.getState().settings.welcome) {
    setShowWelcome(true);
  }
});
```

render cuối App: `{showWelcome && <WelcomeScreen onClose={() => setShowWelcome(false)} />}`.

- [ ] **Step 4: styles.css**:

```css
/* ---- Welcome screen (P21) ---- */
.welcome-overlay {
  position: fixed;
  inset: 0;
  z-index: 30;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(0, 0, 0, 0.55);
}
.welcome-panel {
  position: relative;
  width: 380px;
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 22px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.welcome-title {
  font-size: 20px;
  font-weight: 600;
}
.welcome-sub {
  color: var(--text-dim);
  font-size: 12px;
}
.welcome-actions {
  display: flex;
  gap: 6px;
  flex-wrap: wrap;
}
.welcome-projects {
  display: flex;
  flex-direction: column;
  gap: 4px;
  max-height: 160px;
  overflow-y: auto;
}
.welcome-project {
  text-align: left;
}
.welcome-startup {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 12px;
  color: var(--text-dim);
}
.welcome-close {
  position: absolute;
  top: 8px;
  right: 10px;
}
```

- [ ] **Step 5: e2e init** — 4 file `client/packages/editor/e2e/{smoke,anim,chat,server}.mjs`: NGAY TRƯỚC `await page.goto(BASE);` thêm:

```js
await page.addInitScript(() =>
  localStorage.setItem(
    'spine-editor.settings',
    JSON.stringify({ fps: 30, autosave: true, welcome: false }),
  ),
);
```

`client/packages/mcp-server/e2e/bridge.mjs`: đã có addInitScript (token) — thêm 1 addInitScript nữa cùng nội dung, trước goto.

- [ ] **Step 6: Verify + Commit** — typecheck/build; chạy nhanh `node --check` 5 file e2e; prettier/lint; commit `P21: Welcome screen + shared open/import actions + e2e init` (trailer chuẩn).

---

### Task 6: MCP set_slot_color (63 tools) + keyframe dark + bridge

**Files:**

- Modify: `client/packages/shared/src/index.ts` (BRIDGE_OPS +1), `client/packages/shared/src/tools.ts` (def mới + dark param + comment 63), `client/packages/shared/test/tools.test.ts` (62→63)
- Modify: `client/packages/editor/src/bridge/ops.ts` (case set_slot_color; case set_slot_color_keyframe nhận dark)
- Modify: `client/packages/mcp-server/e2e/bridge.mjs` (bước slotColor + summary)

**Interfaces:** Consumes `SetSlotProperties` color/dark + `UpsertSlotColorKeyframe(anim, slot, key, dark?)` (Task 1).

- [ ] **Step 1: shared** — BRIDGE_OPS: thêm `'set_slot_color',` cạnh `'set_draw_order'`. tools.ts — def mới sau `set_draw_order`:

```ts
def(
  'set_slot_color',
  'Set a slot\'s SETUP color (8-hex rgba) and/or tint-black dark color (6-hex rgb; pass "none" to disable two-color tinting). For animated colors use set_slot_color_keyframe.',
  {
    slot: z.string(),
    color: z.string().optional().describe('8-hex rgba, e.g. "ff8800ff"'),
    dark: z.string().optional().describe('6-hex rgb to enable tint black, or "none" to disable'),
  },
),
```

`set_slot_color_keyframe` def thêm shape field:

```ts
dark: z.string().optional().describe('6-hex rgb — writes an rgba2 (two-color) key'),
```

Comment đầu tools.ts: 62 → 63. tools.test.ts: 3 assertion 62 → 63 và title "62 uniquely named" → 63.

- [ ] **Step 2: ops.ts** — case mới (sau `set_draw_order`):

```ts
case 'set_slot_color': {
  const s = state();
  const slotName = str(params, 'slot');
  const patch: { color?: string; dark?: string | null } = {};
  const color = optStr(params, 'color');
  if (color !== undefined) patch.color = color;
  const dark = optStr(params, 'dark');
  if (dark !== undefined) patch.dark = dark === 'none' || dark === '' ? null : dark;
  if (patch.color === undefined && patch.dark === undefined) {
    throw new Error('Pass "color" and/or "dark".');
  }
  executeOrThrow(new SetSlotProperties(slotName, patch));
  const after = s.doc.findSlot(slotName)!;
  return { color: after.color, dark: after.dark };
}
```

`set_slot_color_keyframe` case: đổi lời gọi cuối thành:

```ts
executeOrThrow(
  new UpsertSlotColorKeyframe(
    str(params, 'animation'),
    str(params, 'slot'),
    key as { color: string },
    optStr(params, 'dark'),
  ),
);
```

- [ ] **Step 3: suites** — `pnpm test` (shared 3 với 63) + typecheck.

- [ ] **Step 4: bridge.mjs** — sau block audio (10b), thêm:

```js
// 10c. Slot setup color + tint black + two-color keyframe (Phase 21).
const colorRes = await call('set_slot_color', {
  slot: flagSlot.slot,
  color: 'ff8800ff',
  dark: '332211',
});
await call('set_slot_color_keyframe', {
  animation: 'flutter',
  slot: flagSlot.slot,
  time: 0.75,
  color: '00ff00ff',
  dark: 'ffffff',
});
const p21Export = JSON.parse((await call('export_spine_json')).json);
const p21Slot = (p21Export.slots ?? []).find((sl) => sl.name === flagSlot.slot);
```

Summary thêm:

```js
slotColorWorks:
  colorRes.color === 'ff8800ff' &&
  colorRes.dark === '332211' &&
  p21Slot?.color === 'ff8800ff' &&
  p21Slot?.dark === '332211' &&
  (p21Export.animations?.flutter?.slots?.[flagSlot.slot]?.rgba2 ?? []).some(
    (k) => k.light === '00ff00ff' && k.dark === 'ffffff',
  ),
```

`node --check` bridge.mjs.

- [ ] **Step 5: Commit** — prettier/lint; `git add client/packages/shared client/packages/editor client/packages/mcp-server`; message `P21: MCP set_slot_color + two-color keyframes — 63 tools` (trailer chuẩn).

---

### Task 7: E2E battery + docs

**Files:** `CLAUDE.md`, `PLAN.md`.

- [ ] **Step 1: Battery chuẩn** (env pnpm shim + CHROMIUM_PATH như các phase trước):

1. `pnpm build` từ client/; kill :4173; vite preview nền; curl 200.
2. smoke.mjs + anim.mjs → `"issues": []` (Welcome đã tắt qua addInitScript).
3. Kill :8017; bridge.mjs → `toolCount: 63`, `slotColorWorks: true`, `audioWorks: true`, `meshEditWorks: true`, setIk/rigFromParts/presetWalk true.
4. Server fake data-dir mới :8100; chat.mjs → `chatRigWorks: true`, `pageErrors: []`.
5. `uv run pytest -q` → 60 passed, 2 skipped. Kill server nền.

- [ ] **Step 2: Docs** — CLAUDE.md sau câu Phase 20: `**Phase 21 done**: Slot Color view (floating Color window, setup color + tint black; dark validate 6-hex, bật/tắt qua null), tint black end-to-end — evaluate rgba2/rgb2 ('computeAnimatedDarkColors') + renderer ColorMatrixFilter per slot; Metrics window; Welcome screen (hiện khi không có autosave, tắt được trong Settings); Settings window (fps 24/30/60 cho step/fields/ghost, autosave toggle); hotkeys B/N/G/C/V/X/Z + guard input đầy đủ (textarea/contentEditable); MCP 'set_slot_color' + 'set_slot_color_keyframe' nhận dark (key rgba2) — **63 MCP tools total**.` — đuôi `Next: PLAN.md §8 phases 21–22.` → `Next: PLAN.md §8 phase 22.` PLAN.md row 21 → `— ✅ (07/2026)`.

- [ ] **Step 3: Format + commit** — prettier CLAUDE.md/PLAN.md; `pnpm format:check`; commit `P21: e2e + docs — Phase 21 complete` (trailer chuẩn).

---

### Final acceptance (spec §8)

- [ ] Core tests mới xanh (slot-color.test.ts); suites + pytest xanh; typecheck/lint/format sạch
- [ ] Battery 4 e2e: `toolCount: 63`, `slotColorWorks: true`, selectors cũ nguyên vẹn
- [ ] Manual: đổi màu + tint black thấy trong viewport; Metrics đúng; Welcome hiện/tắt; FPS đổi ảnh hưởng step; 7 hotkeys chạy và KHÔNG fire khi gõ trong input/textarea
- [ ] Docs (63 tools, row 21 ✅)
