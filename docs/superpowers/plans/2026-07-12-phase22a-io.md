# Phase 22a — Editor IO Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PSD import (layers → assets đặt đúng vị trí), Texture Packer settings (AtlasDialog + trim/POT/padding/maxSize), video WebM + PNG-sequence export, MCP `import_psd` (64 tools).

**Architecture:** Core `atlas.ts` mở rộng options (powerOfTwo, per-region orig/offset cho trim) — TDD được. Editor: `psd-import.ts` chuyển PSD layers thành `SegPartCut[]` rồi tái dùng `importParts` của segment; `frame-export.ts` trích vòng lặp từ gif-export dùng chung cho GIF/WebM/PNG-zip; 2 dialog mới. Fixture `tiny.psd` sinh một lần bằng ag-psd (script scratch, chỉ commit file binary).

**Tech Stack:** deps mới `@webtoon/psd` (MIT, parse PSD thuần JS) + `fflate` (MIT, zip) trong editor package. Không đổi server.

## Global Constraints

- Spec: `docs/superpowers/specs/2026-07-12-phase22a-io-design.md`.
- pnpm từ `client/`; KHÔNG đổi selector/text e2e cũ (nút "Export Atlas", "GIF", menu items cũ giữ nguyên text).
- MCP `export_atlas` giữ default options → assertions bridge cũ (atlasHasRegion) không đổi.
- Core `packAtlas` giữ hành vi mặc định hiện tại (pow2) để core tests cũ pass; editor truyền options explicit.
- Commit message kết thúc `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Prettier trước mỗi commit; `pnpm lint` sạch.

---

### Task 1: Core atlas options — powerOfTwo + trim metadata (orig/offset)

**Files:**

- Modify: `client/packages/core/src/atlas.ts`
- Test: `client/packages/core/test/atlas.test.ts` (thêm describe)

**Interfaces:**

- Hiện có: `packAtlas(inputs: AtlasRegionInput[], options: PackOptions = {}): AtlasLayout` với `PackOptions { maxWidth?, padding? }`; `atlasToText(pngName, layout)` ghi `orig: w, h` + `offset: 0, 0` cứng.
- Produces:
  - `PackOptions` thêm `powerOfTwo?: boolean` (default **true** — giữ hành vi cũ).
  - `AtlasRegionInput`/`AtlasRegionPlacement` thêm optional `origWidth?, origHeight?, offsetX?, offsetY?` (metadata trim — packer chỉ truyền qua).
  - `atlasToText` ghi `orig`/`offset` từ metadata khi có (fallback như cũ).

- [ ] **Step 1: Test fail** — thêm vào cuối `client/packages/core/test/atlas.test.ts`:

```ts
describe('atlas options (P22a)', () => {
  it('powerOfTwo: false keeps exact page size', () => {
    const layout = packAtlas([{ name: 'a', width: 30, height: 20 }], {
      padding: 2,
      powerOfTwo: false,
    });
    expect(layout.width).toBe(34); // 30 + 2*2 padding
    expect(layout.height).toBe(24);
  });

  it('powerOfTwo default rounds up (existing behavior)', () => {
    const layout = packAtlas([{ name: 'a', width: 30, height: 20 }], { padding: 2 });
    expect(layout.width).toBe(64);
    expect(Math.log2(layout.height) % 1).toBe(0);
  });

  it('writes orig/offset from trim metadata', () => {
    const layout = packAtlas(
      [{ name: 'a', width: 10, height: 8, origWidth: 20, origHeight: 16, offsetX: 3, offsetY: 4 }],
      { padding: 0, powerOfTwo: false },
    );
    const text = atlasToText('page.png', layout);
    expect(text).toContain('orig: 20, 16');
    expect(text).toContain('offset: 3, 4');
  });
});
```

(import `packAtlas`, `atlasToText` đã có trong file test — kiểm tra header import, bổ sung nếu thiếu.)

- [ ] **Step 2: RED** — `pnpm --filter @spine-editor/core test -- test/atlas.test.ts` → FAIL (powerOfTwo bị bỏ qua / orig cứng).

- [ ] **Step 3: Implement** — trong `client/packages/core/src/atlas.ts`:

(a) `AtlasRegionInput` (và do đó Placement — placement spread từ input) thêm:

```ts
/** Trim metadata: original (untrimmed) size and top-left offset of the kept pixels. */
origWidth?: number;
origHeight?: number;
offsetX?: number;
offsetY?: number;
```

(b) `PackOptions` thêm `powerOfTwo?: boolean;` — trong `packAtlas`:

```ts
const powerOfTwo = options.powerOfTwo ?? true;
```

và thay 2 chỗ dùng `nextPow2`:

```ts
const pageWidth = Math.min(
  maxWidth,
  powerOfTwo ? nextPow2(widest + padding * 2) : widest + padding * 2,
);
```

(giữ Math.min); cuối hàm:

```ts
const rawHeight = y + shelfHeight + padding;
return { width: pageWidth, height: powerOfTwo ? nextPow2(rawHeight) : rawHeight, regions };
```

LƯU Ý pageWidth khi nhiều region: pageWidth tính từ widest — khi powerOfTwo false và nhiều region, page có thể quá hẹp gây cột dọc; chấp nhận (packer shelf hiện tại; hành vi cũ với pow2 giữ nguyên). Nhưng để width sát hơn khi tắt pow2: sau vòng xếp, tính `usedWidth = max(region.x + region.width) + padding` và trả `width: powerOfTwo ? pageWidth : usedWidth`. Viết đúng như sau (thay return):

```ts
const usedWidth = regions.reduce((m, r) => Math.max(m, r.x + r.width), 0) + padding;
const rawHeight = y + shelfHeight + padding;
return {
  width: powerOfTwo ? pageWidth : usedWidth,
  height: powerOfTwo ? nextPow2(rawHeight) : rawHeight,
  regions,
};
```

(test Step 1: 1 region width 30 pad 2 → usedWidth = (2+30) + 2 = 34 ✓ height = 2+20+2 = 24 ✓.)

(c) `atlasToText` — thay 2 dòng orig/offset:

```ts
`  orig: ${r.origWidth ?? r.width}, ${r.origHeight ?? r.height}`,
`  offset: ${r.offsetX ?? 0}, ${r.offsetY ?? 0}`,
```

- [ ] **Step 4: GREEN** — file test rồi full core suite (`pnpm --filter @spine-editor/core test` — 157 cũ + 3 mới = 160) + `pnpm typecheck`.

- [ ] **Step 5: Commit**

```bash
cd /Users/tuananh/Projects/you/spine_editor/client
pnpm exec prettier --write packages/core/src/atlas.ts packages/core/test/atlas.test.ts
pnpm lint
cd .. && git add client/packages/core
git commit -m "P22a: atlas pack options — powerOfTwo toggle + trim orig/offset metadata

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: Editor buildAtlas options + trim + AtlasDialog

**Files:**

- Modify: `client/packages/editor/src/state/atlas.ts`
- Create: `client/packages/editor/src/components/AtlasDialog.tsx`
- Modify: `client/packages/editor/src/components/Toolbar.tsx` (Export Atlas → dialog)
- Modify: `client/packages/editor/src/styles.css`

**Interfaces:**

- Consumes: Task 1 (`PackOptions.powerOfTwo`, orig/offset metadata).
- Produces: `AtlasOptions { padding: number; maxSize: 1024 | 2048 | 4096; powerOfTwo: boolean; trim: boolean }` + `DEFAULT_ATLAS_OPTIONS` (export từ `state/atlas.ts`); `buildAtlas(assets, pngName, options?: AtlasOptions)`.

- [ ] **Step 1: `state/atlas.ts`** — thay toàn bộ file:

```ts
import {
  atlasToText,
  packAtlas,
  type AtlasRegionInput,
  type AtlasLayout,
} from '@spine-editor/core';
import type { ImageAsset } from './store.js';

export interface BuiltAtlas {
  layout: AtlasLayout;
  atlasText: string;
  pngDataUrl: string;
}

export interface AtlasOptions {
  /** Pixels between packed regions. */
  padding: number;
  /** Maximum page edge in px. */
  maxSize: 1024 | 2048 | 4096;
  /** Round the page size up to powers of two. */
  powerOfTwo: boolean;
  /** Crop transparent borders (writes offset/orig so regions keep their size). */
  trim: boolean;
}

export const DEFAULT_ATLAS_OPTIONS: AtlasOptions = {
  padding: 2,
  maxSize: 2048,
  powerOfTwo: false,
  trim: false,
};

/** Alpha>0 bounding box of a decoded image (whole image when fully opaque). */
function trimBox(img: HTMLImageElement): { x: number; y: number; w: number; h: number } {
  const canvas = document.createElement('canvas');
  canvas.width = img.naturalWidth;
  canvas.height = img.naturalHeight;
  const ctx = canvas.getContext('2d', { willReadFrequently: true });
  if (!ctx) return { x: 0, y: 0, w: img.naturalWidth, h: img.naturalHeight };
  ctx.drawImage(img, 0, 0);
  const { data, width, height } = ctx.getImageData(0, 0, canvas.width, canvas.height);
  let minX = width;
  let minY = height;
  let maxX = -1;
  let maxY = -1;
  for (let yPix = 0; yPix < height; yPix++) {
    for (let xPix = 0; xPix < width; xPix++) {
      if (data[(yPix * width + xPix) * 4 + 3]! > 0) {
        if (xPix < minX) minX = xPix;
        if (xPix > maxX) maxX = xPix;
        if (yPix < minY) minY = yPix;
        if (yPix > maxY) maxY = yPix;
      }
    }
  }
  if (maxX < 0) return { x: 0, y: 0, w: 1, h: 1 }; // fully transparent: keep 1px
  return { x: minX, y: minY, w: maxX - minX + 1, h: maxY - minY + 1 };
}

/** Packs all imported images into one page and composites the PNG. */
export async function buildAtlas(
  assets: ImageAsset[],
  pngName: string,
  options: AtlasOptions = DEFAULT_ATLAS_OPTIONS,
): Promise<BuiltAtlas> {
  if (assets.length === 0) throw new Error('No images imported; nothing to pack.');
  const images = new Map<string, HTMLImageElement>();
  const inputs: AtlasRegionInput[] = [];
  for (const asset of assets) {
    const img = new Image();
    img.src = asset.dataUrl;
    await img.decode();
    images.set(asset.name, img);
    if (options.trim) {
      const box = trimBox(img);
      inputs.push({
        name: asset.name,
        width: box.w,
        height: box.h,
        origWidth: img.naturalWidth,
        origHeight: img.naturalHeight,
        offsetX: box.x,
        // libgdx offset Y is from the BOTTOM of the original image.
        offsetY: img.naturalHeight - (box.y + box.h),
      });
    } else {
      inputs.push({ name: asset.name, width: img.naturalWidth, height: img.naturalHeight });
    }
  }
  const layout = packAtlas(inputs, {
    maxWidth: options.maxSize,
    padding: options.padding,
    powerOfTwo: options.powerOfTwo,
  });
  if (layout.width > options.maxSize || layout.height > options.maxSize) {
    throw new Error(
      `Atlas ${layout.width}x${layout.height} exceeds max size ${options.maxSize}; remove images or raise Max size.`,
    );
  }
  const canvas = document.createElement('canvas');
  canvas.width = layout.width;
  canvas.height = layout.height;
  const ctx = canvas.getContext('2d');
  if (!ctx) throw new Error('Canvas 2D context unavailable.');
  for (const region of layout.regions) {
    const img = images.get(region.name);
    if (!img) continue;
    if (options.trim && region.origWidth !== undefined) {
      const srcY = region.origHeight! - (region.offsetY ?? 0) - region.height;
      ctx.drawImage(
        img,
        region.offsetX ?? 0,
        srcY,
        region.width,
        region.height,
        region.x,
        region.y,
        region.width,
        region.height,
      );
    } else {
      ctx.drawImage(img, region.x, region.y);
    }
  }
  return {
    layout,
    atlasText: atlasToText(pngName, layout),
    pngDataUrl: canvas.toDataURL('image/png'),
  };
}
```

(Chú ý: trimBox trả x,y theo TOP-LEFT; offsetY của libgdx đo từ đáy — công thức trên đảo trục nhất quán ở cả input lẫn drawImage srcY.)

- [ ] **Step 2: AtlasDialog.tsx** (mới):

```tsx
import { useState } from 'react';
import { DEFAULT_ATLAS_OPTIONS, type AtlasOptions } from '../state/atlas.js';

const OPTIONS_KEY = 'spine-editor.atlas-options';

export function loadAtlasOptions(): AtlasOptions {
  try {
    const raw = localStorage.getItem(OPTIONS_KEY);
    if (!raw) return DEFAULT_ATLAS_OPTIONS;
    const parsed = JSON.parse(raw) as Partial<AtlasOptions>;
    return {
      padding: typeof parsed.padding === 'number' ? Math.max(0, Math.min(16, parsed.padding)) : 2,
      maxSize: parsed.maxSize === 1024 || parsed.maxSize === 4096 ? parsed.maxSize : 2048,
      powerOfTwo: parsed.powerOfTwo === true,
      trim: parsed.trim === true,
    };
  } catch {
    return DEFAULT_ATLAS_OPTIONS;
  }
}

/** Texture-packer settings collected before exporting the atlas. */
export function AtlasDialog({
  onExport,
  onClose,
}: {
  onExport: (options: AtlasOptions) => void;
  onClose: () => void;
}) {
  const [options, setOptions] = useState<AtlasOptions>(loadAtlasOptions);
  const patch = (p: Partial<AtlasOptions>) => {
    const next = { ...options, ...p };
    setOptions(next);
    try {
      localStorage.setItem(OPTIONS_KEY, JSON.stringify(next));
    } catch {
      /* no storage */
    }
  };
  return (
    <div className="io-overlay" onClick={onClose}>
      <div className="io-panel" onClick={(e) => e.stopPropagation()}>
        <div className="panel-title">Export Atlas — Texture Packer</div>
        <label className="field">
          <span>Padding</span>
          <input
            type="number"
            min={0}
            max={16}
            value={options.padding}
            onChange={(e) =>
              patch({ padding: Math.max(0, Math.min(16, Math.round(Number(e.target.value)))) })
            }
          />
        </label>
        <label className="field">
          <span>Max size</span>
          <select
            value={options.maxSize}
            onChange={(e) => patch({ maxSize: Number(e.target.value) as 1024 | 2048 | 4096 })}
          >
            {[1024, 2048, 4096].map((s) => (
              <option key={s} value={s}>
                {s}
              </option>
            ))}
          </select>
        </label>
        <label className="field">
          <span>Power of two</span>
          <input
            type="checkbox"
            checked={options.powerOfTwo}
            onChange={(e) => patch({ powerOfTwo: e.target.checked })}
          />
        </label>
        <label className="field">
          <span>Trim transparent edges</span>
          <input
            type="checkbox"
            checked={options.trim}
            onChange={(e) => patch({ trim: e.target.checked })}
          />
        </label>
        <div className="io-actions">
          <button onClick={() => onExport(options)}>Export</button>
          <button onClick={onClose}>Cancel</button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Toolbar** — state `const [showAtlas, setShowAtlas] = useState(false);`; import `AtlasDialog` + `type AtlasOptions`; sửa `onExportAtlas` nhận options:

```ts
async function onExportAtlas(options?: AtlasOptions) {
  const state = useEditor.getState();
  try {
    const built = await buildAtlas(Object.values(state.assets), 'skeleton.png', options);
    downloadText('skeleton.atlas', built.atlasText, 'text/plain');
    downloadDataUrl('skeleton.png', built.pngDataUrl);
  } catch (err) {
    state.setError(err instanceof Error ? err.message : String(err));
  }
}
```

Menu item "Export Atlas" đổi onClick: `onClick={() => setShowAtlas(true)}` (text giữ nguyên). Render: `{showAtlas && (<AtlasDialog onExport={(o) => { setShowAtlas(false); void onExportAtlas(o); }} onClose={() => setShowAtlas(false)} />)}`.

- [ ] **Step 4: styles.css** — cuối file (dùng chung cho cả 2 dialog IO):

```css
/* ---- IO dialogs (P22a) ---- */
.io-overlay {
  position: fixed;
  inset: 0;
  z-index: 30;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(0, 0, 0, 0.55);
}
.io-panel {
  width: 300px;
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.io-panel .field {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
  font-size: 12px;
}
.io-actions {
  display: flex;
  gap: 6px;
  justify-content: flex-end;
  margin-top: 6px;
}
```

- [ ] **Step 5: Verify + Commit** — `pnpm typecheck` + `pnpm --filter @spine-editor/editor build`; prettier/lint; commit:

```bash
cd /Users/tuananh/Projects/you/spine_editor && git add client/packages/editor
git commit -m "P22a: AtlasDialog — padding/maxSize/powerOfTwo/trim texture-packer settings

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: frame-export refactor + WebM + PNG-sequence + ExportAnimationDialog

**Files:**

- Create: `client/packages/editor/src/state/frame-export.ts`
- Modify: `client/packages/editor/src/state/gif-export.ts` (dùng captureFrames)
- Create: `client/packages/editor/src/components/ExportAnimationDialog.tsx`
- Modify: `client/packages/editor/src/components/Toolbar.tsx` (menu item + render)
- Modify: `client/packages/editor/package.json` (dep fflate)

**Interfaces:**

- Produces: `captureFrames(fps, onFrame): Promise<{ width; height; frames }>`; `exportPngSequence(fps, onProgress?): Promise<void>`; `exportWebm(fps, onProgress?): Promise<void>` — `onProgress?: (frame: number, total: number) => void`.

- [ ] **Step 1: dep** — từ client/: `pnpm --filter @spine-editor/editor add fflate`.

- [ ] **Step 2: `state/frame-export.ts`** (mới):

```ts
/**
 * Shared frame-stepping loop for animation exports (GIF/WebM/PNG sequence):
 * steps the current animation on a fixed fps grid, screenshots the viewport
 * per frame, and restores the playhead afterwards.
 */

import { getAnimationDuration } from '@spine-editor/core';
import { zipSync } from 'fflate';
import { bridgeRuntime } from '../bridge/runtime.js';
import { downloadBlob } from './persistence.js';
import { useEditor } from './store.js';

export type FrameProgress = (frame: number, total: number) => void;

export async function captureFrames(
  fps: number,
  onFrame: (img: HTMLImageElement, index: number, total: number) => Promise<void> | void,
): Promise<{ width: number; height: number; frames: number }> {
  const state = useEditor.getState();
  const name = state.anim.current;
  if (!name) throw new Error('Open an animation first.');
  const renderer = bridgeRuntime.renderer;
  const renderNow = bridgeRuntime.renderNow;
  if (!renderer?.ready || !renderNow) throw new Error('Viewport is not ready.');
  const animation = state.doc.getAnimation(name);
  const duration = Math.max(animation ? getAnimationDuration(animation) : 0, 1 / fps);
  const frames = Math.max(1, Math.round(duration * fps));

  const prevTime = state.anim.time;
  const wasPlaying = state.anim.playing;
  useEditor.getState().setPlaying(false);
  let width = 0;
  let height = 0;
  try {
    for (let i = 0; i < frames; i++) {
      useEditor.getState().setAnimTime(i / fps);
      await renderNow();
      const dataUrl = await renderer.screenshot();
      const img = new Image();
      img.src = dataUrl;
      await img.decode();
      width = img.naturalWidth;
      height = img.naturalHeight;
      await onFrame(img, i, frames);
    }
  } finally {
    const s = useEditor.getState();
    s.setAnimTime(prevTime);
    if (wasPlaying) s.setPlaying(true);
    await renderNow();
  }
  return { width, height, frames };
}

/** Exports the current animation as a zip of transparent PNG frames. */
export async function exportPngSequence(fps = 30, onProgress?: FrameProgress): Promise<void> {
  const name = useEditor.getState().anim.current ?? 'animation';
  const canvas = document.createElement('canvas');
  const ctx = canvas.getContext('2d');
  if (!ctx) throw new Error('Canvas 2D context unavailable.');
  const files: Record<string, Uint8Array> = {};
  await captureFrames(fps, async (img, i, total) => {
    onProgress?.(i + 1, total);
    canvas.width = img.naturalWidth;
    canvas.height = img.naturalHeight;
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    ctx.drawImage(img, 0, 0);
    const blob = await new Promise<Blob | null>((resolve) => canvas.toBlob(resolve, 'image/png'));
    if (!blob) throw new Error('PNG encode failed.');
    files[`frame-${String(i + 1).padStart(4, '0')}.png`] = new Uint8Array(await blob.arrayBuffer());
  });
  const zipped = zipSync(files);
  downloadBlob(
    `${name}-frames.zip`,
    new Blob([zipped as unknown as BlobPart], { type: 'application/zip' }),
  );
}

/** Exports the current animation as a WebM video (vp9 → vp8 fallback). */
export async function exportWebm(fps = 30, onProgress?: FrameProgress): Promise<void> {
  const name = useEditor.getState().anim.current ?? 'animation';
  if (typeof MediaRecorder === 'undefined') {
    throw new Error('MediaRecorder is not supported in this browser.');
  }
  const mime = ['video/webm;codecs=vp9', 'video/webm;codecs=vp8', 'video/webm'].find((m) =>
    MediaRecorder.isTypeSupported(m),
  );
  if (!mime) throw new Error('WebM recording is not supported in this browser.');

  const canvas = document.createElement('canvas');
  const ctx = canvas.getContext('2d');
  if (!ctx) throw new Error('Canvas 2D context unavailable.');
  // Manual frame pacing when supported (exact timing); realtime stream otherwise.
  const manual = 'CanvasCaptureMediaStreamTrack' in window;
  const stream = canvas.captureStream(manual ? 0 : fps);
  const track = stream.getVideoTracks()[0] as CanvasCaptureMediaStreamTrack | undefined;
  const recorder = new MediaRecorder(stream, { mimeType: mime });
  const chunks: Blob[] = [];
  recorder.ondataavailable = (e) => {
    if (e.data.size > 0) chunks.push(e.data);
  };
  const stopped = new Promise<void>((resolve) => {
    recorder.onstop = () => resolve();
  });

  let started = false;
  await captureFrames(fps, async (img, i, total) => {
    onProgress?.(i + 1, total);
    if (!started) {
      canvas.width = img.naturalWidth;
      canvas.height = img.naturalHeight;
      recorder.start();
      started = true;
    }
    // Video has no alpha: composite on the viewport background.
    ctx.fillStyle = '#232327';
    ctx.fillRect(0, 0, canvas.width, canvas.height);
    ctx.drawImage(img, 0, 0);
    if (manual && track && 'requestFrame' in track) track.requestFrame();
    await new Promise((r) => setTimeout(r, 1000 / fps));
  });
  recorder.stop();
  await stopped;
  downloadBlob(`${name}.webm`, new Blob(chunks, { type: 'video/webm' }));
}
```

(Kiểu `CanvasCaptureMediaStreamTrack` có trong lib.dom — nếu TS báo thiếu, dùng `as unknown as { requestFrame?: () => void }` cho track và gọi `track.requestFrame?.()`.)

- [ ] **Step 3: gif-export.ts refactor** — thay phần vòng lặp bằng captureFrames, GIỮ nguyên hành vi (nền #232327, quantize, delay):

```ts
import { GIFEncoder, applyPalette, quantize } from 'gifenc';
import { captureFrames, type FrameProgress } from './frame-export.js';
import { downloadBlob } from './persistence.js';
import { useEditor } from './store.js';

export async function exportGif(fps = 20, onProgress?: FrameProgress): Promise<void> {
  const name = useEditor.getState().anim.current;
  if (!name) throw new Error('Open an animation first.');
  const gif = GIFEncoder();
  const canvas = document.createElement('canvas');
  const ctx = canvas.getContext('2d', { willReadFrequently: true });
  if (!ctx) throw new Error('Canvas 2D context unavailable.');
  await captureFrames(fps, (img, i, total) => {
    onProgress?.(i + 1, total);
    if (i === 0) {
      canvas.width = img.naturalWidth;
      canvas.height = img.naturalHeight;
    }
    // GIF has no partial alpha — composite on the viewport background color.
    ctx.fillStyle = '#232327';
    ctx.fillRect(0, 0, canvas.width, canvas.height);
    ctx.drawImage(img, 0, 0);
    const { data, width, height } = ctx.getImageData(0, 0, canvas.width, canvas.height);
    const palette = quantize(data, 256);
    const index = applyPalette(data, palette);
    gif.writeFrame(index, width, height, { palette, delay: Math.round(1000 / fps) });
  });
  gif.finish();
  downloadBlob(
    `${name}.gif`,
    new Blob([gif.bytes() as unknown as BlobPart], { type: 'image/gif' }),
  );
}
```

(getAnimationDuration/bridgeRuntime imports cũ bỏ nếu không còn dùng.)

- [ ] **Step 4: ExportAnimationDialog.tsx** (mới):

```tsx
import { useState } from 'react';
import { exportGif } from '../state/gif-export.js';
import { exportPngSequence, exportWebm } from '../state/frame-export.js';
import { useEditor } from '../state/store.js';

type Format = 'gif' | 'webm' | 'png';

/** Format + fps picker for animation exports (GIF / WebM / PNG sequence). */
export function ExportAnimationDialog({ onClose }: { onClose: () => void }) {
  const [format, setFormat] = useState<Format>('gif');
  const [fps, setFps] = useState(20);
  const [progress, setProgress] = useState<string | null>(null);

  const runExport = () => {
    const onProgress = (f: number, t: number) => setProgress(`Exporting… frame ${f}/${t}`);
    const job =
      format === 'gif'
        ? exportGif(fps, onProgress)
        : format === 'webm'
          ? exportWebm(fps, onProgress)
          : exportPngSequence(fps, onProgress);
    setProgress('Exporting…');
    void job
      .then(onClose)
      .catch((err) =>
        useEditor.getState().setError(err instanceof Error ? err.message : String(err)),
      )
      .finally(() => setProgress(null));
  };

  return (
    <div className="io-overlay" onClick={progress ? undefined : onClose}>
      <div className="io-panel" onClick={(e) => e.stopPropagation()}>
        <div className="panel-title">Export Animation</div>
        <label className="field">
          <span>Format</span>
          <select
            value={format}
            disabled={progress !== null}
            onChange={(e) => {
              const f = e.target.value as Format;
              setFormat(f);
              setFps(f === 'gif' ? 20 : 30);
            }}
          >
            <option value="gif">GIF</option>
            <option value="webm">WebM video</option>
            <option value="png">PNG sequence (zip)</option>
          </select>
        </label>
        <label className="field">
          <span>FPS</span>
          <input
            type="number"
            min={1}
            max={60}
            disabled={progress !== null}
            value={fps}
            onChange={(e) => setFps(Math.max(1, Math.min(60, Math.round(Number(e.target.value)))))}
          />
        </label>
        {progress && <div className="empty">{progress}</div>}
        <div className="io-actions">
          <button disabled={progress !== null} onClick={runExport}>
            Export
          </button>
          <button disabled={progress !== null} onClick={onClose}>
            Cancel
          </button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 5: Toolbar** — state `showExportAnim`; import dialog; menu ☰ thêm item sau "Export Atlas":

```tsx
<button
  disabled={mode !== 'animate' || !useEditor.getState().anim.current}
  onClick={() => setShowExportAnim(true)}
>
  Export Animation…
</button>
```

(mode đã subscribe trong Toolbar; anim.current — thêm selector `const animCurrent = useEditor((s) => s.anim.current);` và dùng nó thay getState trong disabled.) Render `{showExportAnim && <ExportAnimationDialog onClose={() => setShowExportAnim(false)} />}`.

TimelinePanel nút GIF: đổi gọi `exportGif()` giữ nguyên (chữ ký thêm tham số optional — không cần sửa call site).

- [ ] **Step 6: Verify + Commit** — typecheck + build + prettier + lint; commit:

```bash
cd /Users/tuananh/Projects/you/spine_editor && git add client/packages/editor
git commit -m "P22a: WebM + PNG-sequence export via shared captureFrames loop + export dialog

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: PSD import + MCP import_psd (64 tools) + fixture

**Files:**

- Modify: `client/packages/editor/package.json` (dep @webtoon/psd)
- Create: `client/packages/editor/src/state/psd-import.ts`
- Modify: `client/packages/editor/src/components/Toolbar.tsx` (menu Import PSD + input)
- Modify: `client/packages/shared/src/index.ts` + `tools.ts` + `test/tools.test.ts` (64)
- Modify: `client/packages/editor/src/bridge/ops.ts` (case import_psd)
- Create: `client/packages/mcp-server/e2e/fixtures/tiny.psd` (binary, sinh 1 lần)
- Modify: `client/packages/mcp-server/e2e/bridge.mjs` (bước 10d + psdImportWorks)

**Interfaces:**

- Produces: `parsePsdToCuts(buffer: ArrayBuffer): Promise<{ cuts: SegPartCut[]; width: number; height: number }>`; bridge op `import_psd { dataUrl, place_on_canvas? }` trả `{ assets, slots, width, height }`.
- Consumes: `importParts(cuts, { w, h }, placeOnCanvas)` (segment/import-parts.ts), `SegPartCut` (server/api.ts).

- [ ] **Step 1: deps** — `pnpm --filter @spine-editor/editor add @webtoon/psd`.

- [ ] **Step 2: `state/psd-import.ts`**:

```ts
/**
 * PSD import: parses a Photoshop file in the browser (@webtoon/psd) and turns
 * each visible raster layer into a SegPartCut (same shape the segmentation
 * flow uses), so importParts() can add assets + place them on the canvas.
 */

import Psd from '@webtoon/psd';
import type { SegPartCut } from '../server/api.js';

export async function parsePsdToCuts(
  buffer: ArrayBuffer,
): Promise<{ cuts: SegPartCut[]; width: number; height: number }> {
  const psd = Psd.parse(buffer);
  const cuts: SegPartCut[] = [];
  // psd.layers follows the PSD record order (bottom-most layer first), which
  // is exactly the draw order we want when placing parts. (Verified against
  // the committed fixture — if a future library version flips this, the
  // bridge e2e draw-order assertion catches it.)
  for (const layer of psd.layers) {
    if (layer.isHidden || layer.width <= 0 || layer.height <= 0) continue;
    const pixels = await layer.composite();
    const canvas = document.createElement('canvas');
    canvas.width = layer.width;
    canvas.height = layer.height;
    const ctx = canvas.getContext('2d');
    if (!ctx) throw new Error('Canvas 2D context unavailable.');
    ctx.putImageData(new ImageData(new Uint8ClampedArray(pixels), layer.width, layer.height), 0, 0);
    cuts.push({
      name: layer.name?.trim() || 'layer',
      image: canvas.toDataURL('image/png'),
      x: layer.left,
      y: layer.top,
      width: layer.width,
      height: layer.height,
    });
  }
  if (cuts.length === 0) throw new Error('No visible raster layers found in the PSD.');
  return { cuts, width: psd.width, height: psd.height };
}
```

- [ ] **Step 3: Toolbar** — input `.psd` + menu item (sau "Import Atlas"):

```tsx
<button onClick={() => psdInput.current?.click()}>Import PSD</button>
```

ref `const psdInput = useRef<HTMLInputElement | null>(null);`, handler:

```ts
async function onImportPsd(files: FileList | null) {
  const file = files?.[0];
  if (!file) return;
  try {
    const { cuts, width, height } = await parsePsdToCuts(await file.arrayBuffer());
    importParts(cuts, { w: width, h: height }, true);
  } catch (err) {
    useEditor.getState().setError(err instanceof Error ? err.message : String(err));
  }
}
```

input hidden cạnh các input khác:

```tsx
<input
  ref={psdInput}
  type="file"
  accept=".psd"
  hidden
  onChange={(e) => {
    void onImportPsd(e.target.files);
    e.target.value = '';
  }}
/>
```

Imports: `parsePsdToCuts` từ `../state/psd-import.js`, `importParts` từ `../segment/import-parts.js`.

- [ ] **Step 4: shared** — BRIDGE_OPS thêm `'import_psd',` (cạnh `'import_image'`); tools.ts def sau `import_image`:

```ts
def(
  'import_psd',
  'Import a Photoshop PSD (base64 data URL): each visible raster layer becomes an image asset placed at its layer position (bottom layer first = drawn behind). Returns created asset and slot names.',
  {
    dataUrl: z.string().describe('data:image/vnd.adobe.photoshop;base64,... (any data: prefix accepted)'),
    place_on_canvas: z.boolean().optional().describe('Default true'),
  },
),
```

Comment count 63 → 64; tools.test.ts: 3 assertion + title 63 → 64.

- [ ] **Step 5: ops.ts** — case sau `import_image` (import `parsePsdToCuts` + `importParts`):

```ts
case 'import_psd': {
  const dataUrl = str(params, 'dataUrl');
  const comma = dataUrl.indexOf(',');
  if (!dataUrl.startsWith('data:') || comma < 0) {
    throw new Error('Param "dataUrl" must be a base64 data URL of a .psd file.');
  }
  const binary = atob(dataUrl.slice(comma + 1));
  const bytes = new Uint8Array(binary.length);
  for (let i = 0; i < bytes.length; i++) bytes[i] = binary.charCodeAt(i);
  const { cuts, width, height } = await parsePsdToCuts(bytes.buffer);
  const place = params['place_on_canvas'] !== false;
  const result = importParts(cuts, { w: width, h: height }, place);
  return { ...result, width, height };
}
```

- [ ] **Step 6: fixture tiny.psd** — script one-off trong scratchpad (KHÔNG thêm dep vào repo):

```bash
mkdir -p /private/tmp/claude-501/-Users-tuananh-Projects-you-spine-editor/6b990f26-97bc-4e20-b105-3db5aab338c5/scratchpad/psdgen && cd $_
npm init -y >/dev/null && npm i ag-psd >/dev/null
cat > gen.mjs <<'EOF'
import { writePsdBuffer } from 'ag-psd';
import fs from 'node:fs';
const layer = (name, color, x, y, w, h, W, H) => {
  const data = new Uint8ClampedArray(W * H * 4);
  for (let yy = y; yy < y + h; yy++)
    for (let xx = x; xx < x + w; xx++) {
      const o = (yy * W + xx) * 4;
      data[o] = color[0]; data[o + 1] = color[1]; data[o + 2] = color[2]; data[o + 3] = 255;
    }
  return { name, left: 0, top: 0, right: W, bottom: H, imageData: { data, width: W, height: H } };
};
const W = 32, H = 32;
const psd = {
  width: W, height: H,
  children: [
    layer('bg', [40, 80, 200], 0, 0, 32, 32, W, H),   // children[0] = bottom layer
    layer('fg', [220, 60, 60], 8, 8, 12, 12, W, H),
  ],
};
fs.writeFileSync('tiny.psd', writePsdBuffer(psd));
console.log('wrote tiny.psd', fs.statSync('tiny.psd').size, 'bytes');
EOF
node gen.mjs
cp tiny.psd /Users/tuananh/Projects/you/spine_editor/client/packages/mcp-server/e2e/fixtures/tiny.psd
```

(tạo thư mục fixtures trước; ag-psd children[0] = layer ĐÁY theo tài liệu ag-psd. Sau khi bridge chạy, assertion draw-order xác nhận chiều đọc của @webtoon/psd — nếu slots ra ['fg','bg'] thì thêm `.reverse()` vào vòng psd.layers trong psd-import.ts kèm comment, chạy lại bridge.)

- [ ] **Step 7: bridge.mjs** — đầu file có `import fs from 'node:fs'` (đã có); sau block 10c (P21 slot color, tìm `const p21Slot`) thêm:

```js
// 10d. PSD import (Phase 22a).
const psdB64 = fs.readFileSync(new URL('./fixtures/tiny.psd', import.meta.url)).toString('base64');
const psdRes = await call('import_psd', {
  dataUrl: `data:image/vnd.adobe.photoshop;base64,${psdB64}`,
});
const psdState = await call('get_project_state');
const psdSlotOrder = (psdState.spine.slots ?? [])
  .map((sl) => sl.name)
  .filter((n) => psdRes.slots.includes(n));
```

Summary thêm (cạnh slotColorWorks):

```js
psdImportWorks:
  psdRes.assets.length === 2 &&
  psdRes.slots.length === 2 &&
  psdRes.width === 32 &&
  psdRes.assets.every((a) => (psdState.assets ?? []).some((x) => x.name === a)) &&
  psdSlotOrder[0]?.startsWith('bg') &&
  psdSlotOrder[1]?.startsWith('fg'),
```

`node --check` bridge.mjs.

- [ ] **Step 8: suites** — `pnpm test` (shared 3 @64) + `pnpm typecheck`.

- [ ] **Step 9: Commit**

```bash
cd /Users/tuananh/Projects/you/spine_editor
git add client/packages/editor client/packages/shared client/packages/mcp-server client/pnpm-lock.yaml
git commit -m "P22a: PSD import — layers to placed assets + MCP import_psd (64 tools)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: E2E battery + docs

**Files:** `CLAUDE.md`, `PLAN.md`.

- [ ] **Step 1: Battery chuẩn** (env pnpm shim + CHROMIUM_PATH):

1. `pnpm build`; kill :4173; vite preview nền; curl 200.
2. smoke.mjs + anim.mjs → `"issues": []`.
3. Kill :8017; bridge.mjs → `toolCount: 64`, **`psdImportWorks: true`**, slotColorWorks/audioWorks/meshEditWorks/setIk/rigFromParts/presetWalk true, `atlasHasRegion: true` (giữ nguyên).
4. Server fake :8100 data-dir mới; chat.mjs → `chatRigWorks: true`.
5. `uv run pytest -q` → 60 passed, 2 skipped. Kill server nền.

- [ ] **Step 2: Docs** — CLAUDE.md sau câu Phase 21: `**Phase 22a done** (IO slice 1): PSD import ('@webtoon/psd' — layer ảnh hiện → asset đặt đúng vị trí + draw order qua importParts, menu Import PSD, MCP 'import_psd'), Texture Packer settings (AtlasDialog padding/maxSize/powerOfTwo/trim; core packAtlas thêm powerOfTwo + orig/offset trim metadata), export animation WebM + PNG-sequence zip ('fflate') qua vòng captureFrames dùng chung với GIF + Export Animation dialog — **64 MCP tools total**. Còn lại: 22b binary .skel import/export.` — đuôi `Next: PLAN.md §8 phase 22.` → `Next: PLAN.md §8 phase 22b (.skel).` PLAN.md row 22: thêm ` — 22a ✅ (07/2026, còn .skel 22b)` vào cột tên.

- [ ] **Step 3: Format + commit** — prettier CLAUDE.md/PLAN.md; `pnpm format:check`; commit `P22a: e2e + docs — IO slice 1 complete` (trailer chuẩn).

---

### Final acceptance (spec §7)

- [ ] Core 160 tests xanh (3 atlas mới); shared 64; pytest không đổi
- [ ] Battery 4 e2e: `toolCount: 64`, `psdImportWorks: true`, assertions cũ nguyên vẹn
- [ ] Manual: import PSD thật đúng vị trí/draw order; atlas trim có offset/orig; WebM chạy được; PNG zip đủ frame; GIF cũ vẫn hoạt động
- [ ] Docs (64 tools, 22a ✅)
